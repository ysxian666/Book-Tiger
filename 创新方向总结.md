# TIGER 项目创新方向总结

## 研究背景

TIGER 将推荐召回重新表述为生成式检索：先把商品内容编码为 Semantic ID，再训练序列到序列模型自回归生成下一个商品的 Semantic ID。它的优势是可以用生成模型替代大规模向量检索，并潜在利用 Semantic ID 的层次语义提升冷启动和长尾推荐。

本仓库最初尝试在 Amazon Reviews 2023 Books 上复现 TIGER，并提出了两个所谓优化点：码本健康修复、SID 结构变化与用户 token/beam 组合。但当前实验存在以下问题：

- 原始数据只读取前 100 万条评论，最终过滤后仅剩 8,292 条交互；
- catalog 中约 75.65% 商品缺少 metadata；
- collaborative embedding 使用了 valid/test 交互，存在数据泄漏；
- baseline 与原论文训练协议差距过大；
- 优化点 1 同时改变多个变量，不能归因到单一的 error-aware dead-code revival；
- 优化点 2 混合了标准 TIGER 组件、容量扩展、beam 调参和未经证明的 SID 重排；
- 单 seed、低命中人数和 1,192 个测试用户不足以支持稳定的算法结论。

因此，当前最紧迫的任务不是继续叠加模型模块，而是建立可信数据、标准 baseline、严格消融和明确的方法创新。

## 当前项目的研究目标

本项目的总目标是：

> 在干净、可复现、无 valid/test 泄漏的数据上，先建立标准 TIGER 强 baseline，再验证行为簇感知的全局 Semantic ID 分配和用户条件化生成是否能在不增加 SID 长度、词表规模、模型参数量和 beam 预算的前提下提升推荐效果。

具体目标包括：

1. 建立 1M interactions 以上的高质量 Books 5-core 数据子集；
2. 修复 metadata、collaborative embedding 和 cold-start 定义；
3. 复现 L1 标准 TIGER baseline；
4. 在 validation 上建立 L2 tuned baseline；
5. 实现 BC-GSID：Behavior-Cluster-Aware Global SID Assignment；
6. 实现 UCG：User-Conditioned Generation；
7. 验证 BC-GSID、UCG 和二者组合是否具有互补性；
8. 实现 Joint：ranking-aware joint fine-tuning；
9. 在 RTX 5090 上先完成单 seed overnight screening；
10. 后续补齐多 seed、多数据集和论文级统计。

## 核心研究问题

本项目需要回答三个问题：

Q1：在固定 SID 长度、codebook 容量和 item vocabulary 的前提下，行为簇感知的全局 SID 分配是否能降低 semantic drift 并提升 Recall/NDCG？

Q2：在标准 TIGER 已有 hashed user token 的基础上，长期偏好和短期意图组成的动态用户上下文是否能带来额外收益？

Q3：全局 SID 分配和用户条件化生成是否互补？联合训练是否能进一步提高效果？

## 成功标准

数据可信化：

- 不使用前 N 条截断；
- metadata coverage >= 0.9；
- collaborative 只用 train；
- valid/test 不进入 SID 构建；
- cold-start 与 long-tail 分开定义。

方法比较：

- L3 与 L2 使用相同数据、SID 长度、vocab、模型参数量、beam 和训练预算；
- BC-GSID 相对 L2 Recall@20 提升 >= 2%；
- UCG 相对 L2 Recall@20 提升 >= 2%；
- BC-GSID+UCG 相对 L2 提升 >= 3%；
- Joint 相对 BC-GSID+UCG 提升 >= 1.5%；
- random/shuffle 控制不提升；
- semantic drift 不恶化超过 5%。

论文级要求：

- 至少 3 个 seed；
- 最终 5 个 seed；
- test 不参与调参；
- 报告 95% CI；
- 做 paired bootstrap；
- 在至少两个数据集或两个数据规模上成立。

> 目的：将当前项目的优化、调参、baseline 修复和真正算法创新区分开，并给出后续可执行的研究方向。
>
> 核心结论：当前项目的两项优化主要是实验修复、标准组件恢复和超参数调整，尚未形成可辩护的算法创新。下一阶段应优先修复实验有效性，然后只围绕一个明确的方法机制做单变量研究。

---

> **执行版说明（重要）：**  
> 从“第 26 节”开始的内容是后续 Agent 修改代码和进行实验的最终执行规范。若前文与第 26 节以后内容冲突，以后者为准。前文保留用于解释为什么要这样改。执行时不得只做“拼接用户向量”“改 beam”“改 latent”等表面改动，必须按执行版完成数据可信化、标准 baseline、行为感知全局 SID 分配和用户条件化生成。
>
> **三层递进实验体系（最终）：**
>
> - **L1：B1-standard** —— 标准 TIGER baseline；
> - **L2：B1-tuned** —— 仅在 validation 上调参后的强 baseline；
> - **L3：创新方法** —— BC-GSID、UCG、BC-GSID+UCG、Joint，全部建立在 L2 之上。
>
> 第 42 节给出三层递进的最终执行规则。若其他章节与第 42 节冲突，以第 42 节为准。
>
> **RTX 5090 一晚执行档（今晚优先）：**
>
> - 今晚按第 43 节执行；
> - 数据规模：Books 5-core 用户级采样，最终约 1M interactions，top-50K items；
> - 实验：seed=42、L2-lite baseline、BC-GSID、UCG、BC-GSID+UCG、Joint；
> - 评测：2K–3K users，beam/trie 全量，exhaustive 500 users；
> - 目标时间：8–12 小时；
> - 第 43 节是 single-seed overnight screening，不是论文级最终实验。论文级结果仍按第 42 节补齐 L1、L2 调参、多 seed 和全量 test。

## 1. 先区分调参与创新

### 1.1 调参

保持模型结构、训练目标和信息使用方式不变，只改变：

- RQ-VAE 层数；
- codebook 大小；
- latent 维度；
- beam 宽度；
- user bucket 数量；
- usage、entropy、inverse-frequency 权重；
- reset 周期和阈值；
- 训练 epoch、学习率和 batch size。

这些可以改善工程指标，但通常不能作为论文中的算法创新。

### 1.2 baseline 修复

以下内容属于标准 TIGER 协议，不应包装成创新：

- hashed user token；
- 第 4 个唯一化 token；
- collision suffix；
- beam search；
- 统一的召回与排序协议；
- 与原论文一致的 RQ-VAE 训练量和优化器。

### 1.3 真正的算法创新

至少改变以下一项：

- 模型结构；
- 训练目标；
- 信息流和条件机制；
- Semantic ID 分配算法；
- tokenizer 与 generator 的联合训练方式；
- 解码搜索和概率校准；
- 用户、行为与内容信息的融合机制。

创新必须满足：

1. 与原方法使用相同数据；
2. 不借助 test/valid 泄漏；
3. 在相同 SID 长度、模型容量和 decode budget 下比较；
4. 通过消融证明收益来自新机制；
5. 在多个 seed 和合理规模的数据上稳定复现。

---

## 2. 当前项目的准确定性

| 当前改动 | 正确定性 |
|---|---|
| gate fusion | 已有技术，属于方法变体 |
| inverse-frequency loss | 损失加权，方法级改动，但收益不稳 |
| error-aware dead-code reset | 算法机制候选，但已有 K-means reset、Sinkhorn、RQ-KMeans 等近似方案 |
| hard usage、entropy、reset 周期 | 调参 |
| 3 层改 4 层 | 容量扩展，是超参数 |
| latent16 改 latent4 | 超参数 |
| all-level reallocation | 分配算法改动候选，但目前无收益证据 |
| minimal conflict vocab | 工程压缩，不是召回优化 |
| hashed user token | 原论文 baseline 组件 |
| beam5 改 beam20 | 解码超参数 |
| 4×256 + suffix0 | 容量和协议变化 |

因此，当前项目更准确的表述是：

```text
优化点 1 = RQ-VAE 训练修复 + 多组件联合配置
优化点 2 = 标准 TIGER 组件恢复 + beam 调参 + SID 重排启发式
```

具体地，当前 `configs/optimization1.json` 在 baseline 之上同时启用了：

```text
gate fusion
inverse_frequency_power = 0.5
usage_weight = 0.05
entropy_weight = 0.001
hard_usage_weight = 0.2
dead_code_reset = true
dead_code_reset_full_data = true
dead_code_reset_error_aware = true
```

因此，优化点 1 的整体指标变化不能归因于单一 `error-aware dead-code revival`。如果没有分别关闭 gate、逆频、soft usage、entropy、hard usage、full-data reset 和 error-aware reset 的单变量消融，就只能表述为“多组件码本健康组合有效”，不能表述为“dead-code revival 有效”。

---

## 3. 创新建议总览

按优先级排序：

1. 先修复数据与实验协议；
2. 再做用户条件化 Semantic ID 分配；
3. 研究内容 SID 与行为 SID 的双流融合；
4. 推进 tokenizer 与 generator 的联合训练；
5. 优化生成式排序与概率校准；
6. 最后再考虑用户向量、协同向量和多模态信息的深度融合。

用户向量直接拼接只能作为 baseline，不是主要创新。

---

## 4. 方向一：用户条件化 Semantic ID 分配

### 4.1 核心想法

当前 SID 是全局固定的：

```text
item -> SID
```

进一步可定义为：

```text
P(SID | item, user_context)
```

即同一个 item 可以在不同用户意图下选择不同的量化路径，但全局 item vocabulary 和 SID 长度保持不变。

### 4.2 可实现的机制

- 用户历史生成长期偏好向量和短期意图向量；
- 用户意图控制 RQ-VAE 各层 codebook 的 routing 或 gating；
- 用户条件参与冲突重分配；
- 用户条件作为软约束，影响候选 SID 的语义漂移评分；
- 保持全局 SID lookup 不变，避免破坏检索系统接口。

### 4.3 为什么比简单拼接更强

简单拼接到 input 只是增加特征；用户条件化分配改变的是：

- SID 的学习目标；
- item 表示形成过程；
- 冲突处理方式；
- 语义层级与用户意图的关系。

### 4.4 主要风险

- 条件化 SID 可能导致同一 item 无法稳定回表；
- 用户路由容易退化为用户 ID 记忆；
- 训练集外用户缺少稳定条件；
- 必须避免使用 valid/test 交互构造用户条件。

---

## 5. 方向二：内容 SID 与行为 SID 双流融合

### 5.1 核心想法

分别学习：

```text
内容 SID：来自文本、图像、类目和属性
行为 SID：来自用户—物品共现和序列转移
```

生成时同时预测两条 SID 流，再进行对齐、融合和排序。

### 5.2 可能的融合方式

- 双流 cross-attention；
- 内容 SID 作为行为 SID 的语义约束；
- 行为 SID 作为内容 SID 的协同校准；
- 冷启动 item 偏重内容流；
- 短历史用户偏重用户/行为流；
- 两流不一致时使用门控或对比学习处理。

### 5.3 创新风险

EAGER 等已有 behavior-semantic 双流工作，LC-Rec 也已研究协同语义融合。因此仅做双流拼接不够，必须提出新的：

- 对齐目标；
- 冲突消解机制；
- 冷启动路由策略；
- 或跨用户/跨域泛化机制。

---

## 6. 方向三：Tokenizer 与 Generator 联合训练

### 6.1 当前问题

TIGER 采用两阶段：

```text
先训练 RQ-VAE
-> 冻结 SID
-> 再训练生成模型
```

RQ-VAE 优化重建误差，而 generator 优化下一个 SID 的条件概率，两者存在目标错位。

### 6.2 可定义的联合目标

```text
L_total =
    L_reconstruction
  + alpha * L_commitment
  + beta  * L_code_usage
  + gamma * L_next_item_generation
  + delta * L_ranking
  + eta   * L_user_item_alignment
  + zeta  * L_prefix_consistency
```

可用技术包括：

- Gumbel-Softmax；
- straight-through estimator；
- soft-to-hard quantization；
- listwise ranking loss；
- contrastive alignment；
- REINFORCE 或可微 beam 近似。

### 6.3 为什么是创新

它把“为重建优化的 tokenizer”改成“为下游推荐优化的 tokenizer”，改变了学习目标，而不是只改超参数。

LETTER 和 end-to-end item tokenization 已有相关工作，因此需要提出更具体的贡献，例如：

- 排序感知的 tokenizer；
- 去偏的协同 tokenizer；
- 用户条件化 tokenizer；
- 带冲突感知的结构化量化目标。

---

## 7. 方向四：个性化生成与解码校准

### 7.1 当前问题

即使 encoder 使用用户向量，最终 beam search 仍可能主要依赖：

```text
log P(SID | history)
```

用户信息对最终排序的作用有限，且 beam search 可能引入 popularity、长度和前缀偏置。

### 7.2 可定义的新评分

```text
score(SID, user) =
    log P(SID | user, history)
  + lambda1 * alignment(user_vector, item_embedding)
  - lambda2 * popularity_prior(item)
  - lambda3 * length_penalty(SID)
  + lambda4 * diversity_bonus(SID, selected_set)
```

### 7.3 创新点

- 将用户条件带入解码评分，而不只是 encoder；
- 对生成概率进行 popularity 校准；
- 用 listwise ranking 直接优化 Top-K；
- 区分“命中率提高”和“排序质量提高”；
- 同时评估 beam 与 exhaustive ranking 的一致性。

---

## 8. 方向五：用户向量与 SID 的条件融合

### 8.1 简单拼接

```text
query = concat(project(user_vector), SID_history_embeddings)
```

这是普通 feature fusion，只能作为 baseline。

### 8.2 更强的融合方式

- 用户向量作为 query，历史 SID 作为 key/value 做 cross-attention；
- 长期用户向量和短期意图向量分开建模；
- FiLM 或 gating 调制每一层 SID embedding；
- 用户向量参与 decoder 每一层的 cross-attention；
- 用户向量控制 SID codebook routing；
- 用户向量参与候选打分和重排。

### 8.3 什么时候可能有效

- 用户历史很短；
- 同一历史对应多种下一 item；
- 用户长期偏好稳定；
- 商品内容语义较强；
- 冷启动 item 需要用户意图辅助判断；
- 用户表示来自训练期且无泄漏。

### 8.4 什么时候可能无效

- 用户向量只是 user ID embedding；
- 用户数量很少，模型直接记忆；
- 用户向量与历史 SID 信息重复；
- 用户向量包含 test/valid 信号；
- 收益只出现在头部用户；
- beam 和模型参数同时变大，无法归因。

现有工作已经覆盖部分方向：

- EAGER：behavior-semantic 双流协作；
- LETTER：可学习 item tokenization；
- LC-Rec：协同语义融入生成式推荐。

因此，“SID 与用户向量直接拼接”通常不足以构成创新，必须提出新的条件化、对齐或联合训练机制。

---

## 9. 推荐优先级

### 第一优先级：实验可信化

- 随机抽样或完整 5-core Books；
- 修复 metadata 缺失；
- collaborative embedding 只用 train interactions；
- valid/test 构建前冻结 item representation；
- 使用真实 unseen item 定义 cold-start。

### 第二优先级：固定预算的 SID 分配创新

在不增加 SID 长度、码本容量和模型参数的条件下，提出全局 SID 分配算法：

```text
min reconstruction_error
  + semantic_drift
  + collision_penalty
  + code_usage_imbalance
  + prefix_inconsistency
```

### 第三优先级：用户条件化 tokenizer 或双流 SID

这是最有可能形成方法论文的方向，但必须先证明不是已有工作的简单复刻。

### 第四优先级：tokenizer-generator 联合训练

研究价值高，但实现难度和训练成本也高。

### 第五优先级：个性化 beam 评分与概率校准

适合与上述任一方向组合，单独做容易被视为调参。

---

## 10. 最小可行实验矩阵

| 实验 | 目的 |
|---|---|
| 标准 TIGER baseline | 确定可信参照 |
| + 原论文 user token | 禁止把原论文组件当创新 |
| + train-only collaborative features | 验证协同信息是否真正有帮助 |
| + 用户向量 concat | 作为最小 fusion baseline |
| + 用户向量 cross-attention | 测试融合位置是否重要 |
| + 用户条件化 SID allocation | 测试核心创新 |
| + 内容/行为双流 SID | 测试多流协作 |
| + tokenizer-generator 联合训练 | 测试目标对齐 |
| + 个性化 beam 校准 | 测试排序改进 |
| random user vector | 负向对照 |
| shuffled user vector | 检查模型是否真正使用用户信息 |
| random semantic ID | 检查 SID 语义结构贡献 |
| 相同容量和 beam 的对照 | 排除算力和容量影响 |

所有实验必须固定：

- 数据划分；
- SID 长度；
- item vocabulary；
- 模型参数量；
- beam 预算；
- 训练步数；
- 评测协议。

---

## 11. 验收标准

一个创新点只有同时满足以下条件，才值得作为主贡献：

1. 相对标准 TIGER baseline 有稳定提升；
2. 多个 seed 的均值提升超过 seed-noise；
3. 与容量匹配的对照相比仍然有效；
4. 消融能定位到具体机制；
5. 随机向量、打乱向量等负向对照无效；
6. 短历史、长尾和 cold-start 分组有合理解释；
7. 不依赖 test/valid 泄漏；
8. exhaustive ranking 与 beam ranking 结论一致；
9. 推理成本可接受；
10. 在至少两个数据集或两个显著不同的数据规模上成立。

---

## 12. 最终建议

当前项目不应继续把以下内容称为创新：

- beam 调大；
- user bucket 改数量；
- latent 和 RQ-VAE 层数调整；
- 最小冲突词表；
- 简单用户向量拼接；
- 原论文已经存在的 user token 和 collision token。

优先研究的真实方向是：

1. 固定 token 预算下的用户条件化 SID 分配；
2. 内容 SID 与行为 SID 的对齐式双流生成；
3. tokenizer 与 generator 的排序感知联合训练；
4. 带 popularity 去偏和用户条件的解码校准。

最具论文潜力、又最符合当前项目基础的方向是：

> 在不增加 SID 长度、不扩大 item vocabulary、不增加 decode budget 的前提下，引入用户条件化 SID 分配，并通过联合目标让 SID 同时满足语义保真、码本均衡和下游推荐性能。
---

# 后续创新实施步骤：交给 Agent 的代码改造蓝图

> 本节是后续 Agent 修改代码时必须优先遵守的执行计划。
>
> 总原则：先修复实验可信度，再实现一个真正的方法创新，禁止把“调 beam、改 latent、加层数、恢复 user token”当作最终创新。
>
> 推荐主线：**用户条件化 SID 分配 + 动态用户上下文融合**。
>
> 实施顺序必须是：数据可信化 -> 标准 baseline -> 用户上下文融合 -> 用户/行为条件化 SID 分配 -> 联合训练与解码校准。每一步通过验收后再进入下一步，禁止一次性堆叠所有改动。

---

## 13. Agent 的硬性约束

后续 Agent 修改代码时必须遵守：

1. 不得使用 valid/test 交互构造 collaborative embedding、用户画像或 SID 分配目标。
2. 不得改变训练集、验证集和测试集的切分协议。
3. 不得通过扩大 SID 长度、扩大 codebook 容量或增大 beam 来代替算法创新。
4. baseline 与候选方法必须使用相同：
   - 数据；
   - item vocabulary；
   - SID 长度；
   - 模型参数量；
   - 训练步数；
   - beam 预算；
   - 硬件与 batch size。
5. 每个新功能必须有配置开关，默认关闭，保证旧 baseline 可复现。
6. 每个阶段必须形成独立 commit、实验配置、日志和结果报告。
7. 只有通过验收门槛的结果，才允许写入 README 或论文结论。
8. 所有指标必须报告命中人数、置信区间和至少 3 个 seed。
9. test 集只能用于最终评估，不能用于选择模型、seed 或超参数。
10. 如果实现后效果不成立，要如实记录负结果，不得只保留最佳 seed。

---

## 14. 总体实施路线

推荐按照以下六个阶段执行。

```text
Phase 0：数据与协议审计
Phase 1：标准 TIGER baseline
Phase 2：动态用户上下文表示与融合
Phase 3：行为感知的全局 SID 分配
Phase 4：用户条件化 SID 分配与生成联合验证
Phase 5：解码校准与可选联合训练
Phase 6：扩展实验与论文级确认
```

其中：

- Phase 0–1 不是创新，是可信实验的前置条件；
- Phase 2 是用户信息融合基础；
- Phase 3 是第一个真正可发表的方法候选；
- Phase 4 是推荐的主创新；
- Phase 5 是增强模块；
- Phase 6 决定是否能够形成稳定贡献。

---

## 15. Phase 0：数据与协议审计

### 15.1 目标

消除数据泄漏、metadata 缺失和评估样本过小造成的问题，建立可信实验基础。

### 15.2 必做改动

#### A. 修正抽样方式

当前 `max_reviews=1000000` 是读取前 100 万条，不是随机抽样。

需要新增：

```json
{
  "data": {
    "sampling_mode": "random",
    "sample_seed": 42,
    "sample_size": 1000000
  }
}
```

要求：

- 对 review 记录做稳定哈希或统一随机抽样；
- 对 metadata 使用完整数据流，不要独立取前 100 万条；
- 记录抽样前后的用户、物品、交互统计；
- 输出 `data/audit/data_sampling_report.json`。

#### B. 修复 metadata 覆盖

要求 Agent 增加数据审计：

```text
metadata_coverage =
    有有效 title/description/category 的 catalog item 数
  / catalog item 总数
```

建议规则：

- metadata coverage < 90% 时，不允许把当前结果称为 semantic-ID 实验；
- missing metadata item 必须显式标记；
- 禁止静默使用 ASIN 代替内容；
- 可提供三种模式：
  - `exclude_missing_metadata`；
  - `learned_missing_token`；
  - `multimodal_or_llm_augmented_metadata`。

输出：

```text
data/audit/metadata_coverage.json
data/audit/missing_metadata_items.parquet
```

#### C. collaborative embedding 只用 train interactions

修改 `code/tiger_rec/features/collaborative.py`，使数据来源不固定为 `interactions.parquet`，而是显式传入：

```text
train_interactions.parquet
```

新增配置：

```json
{
  "collaborative": {
    "interaction_source": "train_only",
    "allow_valid_interactions": false,
    "allow_test_interactions": false
  }
}
```

必须加入测试：

- build 过程中读取 test/valid 时直接报错；
- collaborative metadata 记录 source split；
- valid/test item 的表示在建 SID 前冻结。

#### D. 冻结 item representation

流程调整成：

```text
读取所有 catalog
-> 使用 train interactions 构造协同特征
-> 训练 RQ-VAE / 生成 SID
-> 冻结 semantic_ids.json 和 token_space
-> 再构造 valid/test 生成器样本
-> 最后执行 test 评测
```

禁止：

```text
先看 test 用户行为 -> 调整用户画像 -> 调整 SID -> 再评测
```

#### E. 真实 cold-start 定义

删除 `train_count <= 3` 这种全覆盖式定义。

建议定义：

- `cold_item`：训练期完全没有交互，且第一次出现在 valid/test 的物品；
- `few_shot_item`：训练期交互数为 1–5；
- `long_tail_item`：训练期频次处于后 20%，但必须与 cold 分开。

输出分组统计，而不是把 97% 物品都称为 cold-start。

### 15.3 Phase 0 验收

必须全部满足：

- metadata coverage 报告存在；
- collaborative source 只能是 train；
- valid/test 不参与 item representation；
- cold-start 分组有区分度；
- 数据审计可以复现；
- 旧配置的新增字段有默认值，不破坏 baseline。

---

## 16. Phase 1：标准 TIGER baseline

### 16.1 目标

先建立一个可信、公平、可复现的标准 TIGER baseline。

### 16.2 对齐项

- RQ-VAE：3 层语义码 + 1 个唯一化 token；
- 每层 codebook 256；
- latent 32；
- 第 4 个 token 必须进入生成器 vocabulary；
- 使用 hashed user token；
- 使用 beam search；
- baseline 与候选模型使用同一 beam 协议；
- 标准 beam 允许生成全部 code token 和 conflict token，再过滤无效 SID；
- trie-constrained beam 作为单独变体，不作为唯一主结果。

### 16.3 标准 baseline 配置

建议新增：

```json
{
  "sid": {
    "num_semantic_levels": 3,
    "codebook_size": 256,
    "latent_dim": 32,
    "length_mode": "fixed",
    "collision_mode": "suffix",
    "assignment_mode": "greedy"
  },
  "generator": {
    "use_user_tokens": true,
    "num_user_buckets": 2000,
    "use_user_context": false,
    "num_beams": 20
  }
}
```

### 16.4 必做对照

| 配置 | 目的 |
|---|---|
| standard baseline | 主参照 |
| standard beam + conflict token | 忠实复现解码协议 |
| trie beam | 单独衡量约束效果 |
| no user token | 不得作为主 baseline，只作为消融 |
| beam 20/50 | 通过 validation 选择，禁止用 test 择优 |

### 16.5 评估

必须报告：

- Recall@5/10/20；
- NDCG@5/10/20；
- MRR；
- coverage；
- invalid SID rate；
- 命中人数；
- 95% 置信区间；
- 5 个 seed 的均值与标准差；
- exhaustive ranking 与 beam ranking 对照。

### 16.6 Phase 1 验收

- baseline 在验证集上稳定；
- test 不参与任何选择；
- 所有组件可单变量关闭；
- 旧 quick 结果的结论可以重新核对；
- 如果 13M 参数无法实现，必须明确记录容量差异。

---

## 17. Phase 2：动态用户上下文表示与融合

### 17.1 目标

在不修改 SID 结构的前提下，验证用户上下文是否真的提供额外信息。

### 17.2 用户上下文的构成

每个训练样本只能使用目标之前的历史：

```text
history SIDs
history ratings
history timestamps
最近 K 个物品
长期历史聚合
```

用户上下文分成：

- 长期偏好向量 `u_long`；
- 短期意图向量 `u_short`；
- 历史序列状态 `H`。

### 17.3 推荐模型接口

新增：

```python
class UserContextEncoder(nn.Module):
    def forward(
        self,
        history_sid_tokens,
        history_ratings,
        history_timestamps,
        attention_mask,
    ) -> tuple[Tensor, Tensor]:
        # 返回 u_long, u_short
        ...
```

在 `code/tiger_rec/models/generator.py` 中新增：

```python
class UserConditionedT5(nn.Module):
    def forward(...):
        # 1. 编码历史 SID
        # 2. 生成 u_long, u_short
        # 3. 将用户软 token 拼接到 encoder inputs_embeds
        # 4. 执行 T5 generation
        ...
```

### 17.4 融合方式分阶段实现

按以下顺序做，不要一次全部实现：

1. `no_user_context`；
2. `hashed_user_token`；
3. `user_concat`；
4. `user_soft_prefix`；
5. `user_cross_attention`。

其中：

- `user_concat` 只是 baseline；
- `user_soft_prefix` 是重点；
- `user_cross_attention` 是增强实验。

### 17.5 配置接口

```json
{
  "user_context": {
    "enabled": true,
    "window": 20,
    "long_term_window": 100,
    "embed_dim": 128,
    "fusion": "soft_prefix",
    "num_soft_tokens": 2,
    "dropout": 0.1,
    "use_user_token": true
  }
}
```

### 17.6 必做负向对照

- random user vector；
- shuffled user vector；
- average user vector；
- user vector 只在 encoder；
- user vector 只在 decoder；
- user vector 同时参与两者；
- 短历史用户和长历史用户分组评估。

判定标准：

- shuffled user vector 不降反升，说明没有真正使用用户信息；
- 只有头部用户提升，说明是流行度或用户记忆；
- 只有在短历史用户上提升，才可能是真正个性化。

### 17.7 Phase 2 验收

如果 `user_soft_prefix` 相对 `hashed_user_token` 的提升小于 seed noise，则：

- 不得把用户向量融合作为主创新；
- 可以保留为辅助模块；
- 进入 Phase 3。

---

## 18. Phase 3：行为感知的全局 SID 分配

### 18.1 目标

在不增加 SID 长度、不扩大 codebook、不增加 beam 的前提下，减少 raw collision 和 semantic drift。

这是推荐的第一个真正算法创新点。

### 18.2 当前问题

当前流程：

```text
RQ-VAE 生成 code
-> 冲突 item 查询 top-k
-> 找到第一个空闲 code
-> 否则加 suffix
```

缺点：

- 只看 code 是否空闲；
- 不看行为相似性；
- 不保证语义保真；
- 不最小化全局 semantic drift；
- 用户/行为信息没有参与分配。

### 18.3 新方法：Behavior-Aware Global SID Assignment

保持全局 item vocabulary 不变，每个 item 仍然只对应一个 SID。  
但 SID 的选择不再由局部 greedy 决定，而是通过全局目标函数求解。

对每个 item `i` 和候选 SID `s` 定义：

```text
E(i, s) =
    lambda_recon * reconstruction_error(i, s)
  + lambda_semantic * semantic_drift(i, s)
  + lambda_behavior * behavior_cost(i, s)
  + lambda_usage * code_usage_imbalance(s)
  + lambda_collision * collision_penalty(s)
```

其中：

- `reconstruction_error`：量化重建误差；
- `semantic_drift`：候选 SID 与原始 latent/内容向量的距离；
- `behavior_cost`：候选 SID 与 item 行为画像的距离；
- `code_usage_imbalance`：码本使用偏置；
- `collision_penalty`：同一 SID 被多个 item 使用。

### 18.4 候选 SID 生成

对每个 item：

```text
level 0: top-k candidates
level 1: top-k candidates
level 2: top-k candidates
suffix:  0, 1, ..., max_suffix
```

只枚举 top-k 组合，避免全空间搜索。

### 18.5 分配求解

推荐实现顺序：

1. 先实现 `greedy_behavior_aware`，用于快速验证；
2. 再实现 `min_cost_flow` 或 `Hungarian` 全局分配；
3. 如果规模大，再实现分层 Sinkhorn 或 Lagrangian relaxation。

新增模块：

```text
code/tiger_rec/semantic_id/assignment.py
```

核心接口：

```python
def assign_semantic_ids(
    item_ids,
    base_codes,
    topk_codes,
    codebooks,
    item_content_latents,
    item_behavior_latents,
    token_space,
    config,
) -> AssignmentResult:
    ...
```

### 18.6 行为画像

`item_behavior_latents` 只能由训练集构造：

- item-user 共现；
- item-item 共现；
- 行为 SVD；
- 训练期序列转移统计；
- 不使用 valid/test。

对于 train_count=0 的 item：

- behavior 分支置零或使用缺失 token；
- 由 content/reconstruction 分支主导；
- 不允许使用 test 交互补全行为画像。

### 18.7 配置接口

```json
{
  "sid_allocation": {
    "mode": "global_behavior_aware",
    "solver": "min_cost_flow",
    "candidate_topk": 8,
    "lambda_recon": 1.0,
    "lambda_semantic": 0.5,
    "lambda_behavior": 0.2,
    "lambda_usage": 0.05,
    "lambda_collision": 10.0,
    "max_suffix_tokens": 16
  }
}
```

### 18.8 必做对照

| 方法 | 目的 |
|---|---|
| greedy last-level suffix | 当前 baseline |
| all-level greedy reallocation | 现有优化点 2 |
| behavior-aware greedy | 验证行为成本 |
| global behavior-aware assignment | 验证全局最优分配 |
| random behavior latent | 负向对照 |
| content-only assignment | 消融行为信息 |
| behavior-only assignment | 消融内容信息 |
| 相同 SID 长度和容量 | 公平比较 |

### 18.9 验收标准

必须满足：

- SID 长度不增加；
- codebook 容量不增加；
- behavior latent 不含 valid/test；
- semantic drift 不恶化，或恶化幅度有界；
- raw collision 降低；
- Recall/NDCG 超过 seed noise；
- 在短历史用户和冷 item 分组上有合理解释；
- 在全局分配与 greedy 之间有稳定差异。

如果只降低 collision 但没有推荐提升，只能称为“SID 结构优化”，不能称为“推荐优化”。

---

## 19. Phase 4：用户条件化 SID 分配的联合验证

### 19.1 目标

把用户上下文从生成器端扩展到 SID 分配端，验证是否能形成真正的方法创新。

### 19.2 两种实现方式

#### 方案 A：用户群体条件化

- 将用户按可泛化行为聚成若干 cluster；
- 每个 cluster 使用不同 SID 分配权重；
- 仍保持全局 item vocabulary 和 SID 唯一性；
- 在推理时根据用户 cluster 选择对应分配偏好。

这种方式比动态 SID 更容易保持回表结构。

#### 方案 B：用户样本条件化

- 每个训练样本根据历史生成 `u_context`；
- `u_context` 影响行为成本；
- 训练时使用 user-conditioned allocation；
- 推理时使用用户上下文进行候选打分；
- 全局 SID lookup 保持不变。

### 19.3 关键约束

禁止每个用户拥有独立 SID vocabulary。  
这会导致：

- SID 空间爆炸；
- item vocabulary 不稳定；
- 无法共享语义；
- 小用户量下严重记忆。

### 19.4 推荐训练目标

```text
L_total =
    L_recon
  + alpha * L_commitment
  + beta  * L_usage
  + gamma * L_semantic
  + delta * L_behavior
  + epsilon * L_next_item
  + zeta  * L_ranking
```

其中 `L_next_item` 和 `L_ranking` 是下游目标，不能只优化 RQ-VAE 重建。

### 19.5 必做消融

- 只优化 reconstruction；
- 只加 behavior；
- 只加 user context；
- behavior + user context；
- behavior + user context + ranking；
- random user context；
- shuffled user context；
- standard TIGER baseline。

### 19.6 Phase 4 验收

只有同时满足以下条件，才可称为主创新：

1. 相对标准 baseline 提升稳定；
2. 相对 Phase 2 的用户融合也有提升；
3. 相对 Phase 3 的 behavior-aware 分配有提升；
4. random/shuffled context 无效；
5. 容量、SID 长度和 beam 完全匹配；
6. 至少两个数据规模上成立；
7. 短历史/长尾/cold 分组有稳定收益。

---

## 20. Phase 5：解码校准与可选联合训练

### 20.1 个性化解码评分

新增解码评分接口：

```text
score(s, u) =
    log P(s | history, u)
  + lambda_align * alignment(u, item_embedding)
  - lambda_pop * popularity_prior(item)
  - lambda_len * length_penalty(s)
  + lambda_div * diversity_bonus(s, selected_set)
```

必须对比：

- 原始 log-prob；
- length-normalized；
- user-conditioned；
- popularity-debiased；
- listwise reranking；
- trie 与 untriized。

### 20.2 tokenizer-generator 联合训练

可选实现：

- Gumbel-Softmax；
- straight-through；
- soft-to-hard quantization；
- listwise ranking loss；
- user-item contrastive alignment；
- prefix consistency regularization。

新增配置：

```json
{
  "joint_training": {
    "enabled": true,
    "warmup_rqvae_epochs": 20,
    "lambda_next_item": 1.0,
    "lambda_ranking": 0.2,
    "lambda_align": 0.1,
    "lambda_prefix": 0.05,
    "freeze_codebooks_epochs": 5
  }
}
```

### 20.3 Phase 5 验收

- 联合训练不能破坏 SID 唯一性；
- SID lookup 仍可稳定回表；
- 训练稳定性不能低于 baseline；
- 提升不能来自 beam 或模型容量；
- 必须报告 SID fidelity、prefix purity、无效率和时延。

---

## 21. Phase 6：扩展实验与论文级确认

### 21.1 数据集

至少建议：

- Amazon Reviews 2014 Beauty；
- Amazon Reviews 2014 Sports 或 Toys；
- Amazon Reviews 2023 Books 的全量或高质量 5-core 子集。

### 21.2 基线

至少包括：

- Popularity；
- ItemCF；
- SASRec；
- BERT4Rec 或 HSTU；
- 标准 TIGER；
- random Semantic ID + 同 generator；
- LC-Rec / LETTER / EAGER 风格强基线（若资源允许）。

### 21.3 统计

- 至少 5 个 seed；
- 报告均值、标准差、置信区间；
- 做 paired bootstrap 或 paired permutation test；
- 明确区分 validation 选择和 test 评测；
- 禁止只报告最佳 seed。

### 21.4 论文级消融

- SID 长度固定；
- codebook 容量固定；
- generator 参数量固定；
- beam 固定；
- user token 固定；
- 只改变行为感知分配或用户条件化机制。

---

## 22. 建议的代码文件改动清单

### 数据与配置

- `code/tiger_rec/config.py`
  - 新增 `UserContextConfig`；
  - 新增 `SIDAllocationConfig`；
  - 新增 `JointTrainingConfig`；
  - 修改 `GeneratorConfig`，增加 `use_user_context`、`user_fusion`、`num_soft_tokens`。
- `data/SOURCE.md`
  - 更新采样、metadata coverage、cold-start 定义。
- `configs/`
  - 新增 baseline、user-fusion、behavior-allocation、joint-training 配置。

### 数据与特征

- `code/tiger_rec/data/preprocess.py`
  - 随机抽样；
  - metadata 审计；
  - train-only collaborative source。
- `code/tiger_rec/features/collaborative.py`
  - 禁止 valid/test 参与；
  - 支持 behavior embedding。
- `code/tiger_rec/data/dataset.py`
  - 生成动态 `u_long`、`u_short` 所需数据；
  - 严格使用 prefix history。

### 模型

- `code/tiger_rec/models/generator.py`
  - 新增 `UserContextEncoder`；
  - 新增 `UserConditionedT5`；
  - 支持 soft prefix、cross-attention、gating。
- `code/tiger_rec/semantic_id/rqvae.py`
  - 支持行为感知目标；
  - 支持用户条件化 codebook routing 或 alignment。
- `code/tiger_rec/semantic_id/assignment.py`
  - 新增全局 SID 分配；
  - 实现 greedy、min-cost-flow、可选 Sinkhorn。
- `code/tiger_rec/semantic_id/build.py`
  - 调用新分配器；
  - 输出 semantic fidelity 和 behavior alignment 报告。

### 解码与评估

- `code/tiger_rec/decoding/constrained_beam.py`
  - 支持 user-conditioned score；
  - 支持 popularity calibration；
  - 保持 full-token 标准 beam。
- `code/tiger_rec/evaluation/protocol.py`
  - 输出命中人数、置信区间、分组指标。
- `code/tiger_rec/evaluation/metrics.py`
  - 新增 semantic drift、prefix purity、NMI、behavior alignment。

### 测试

- `code/tests/test_no_leakage.py`
- `code/tests/test_user_context.py`
- `code/tests/test_sid_assignment.py`
- `code/tests/test_user_conditioned_generator.py`
- `code/tests/test_metrics_groups.py`

---

## 23. 每个阶段的交付物

每个 Phase 都必须提交：

```text
configs/experiments/<phase>.json
results/<phase>/metrics.json
results/<phase>/seed_variance.json
results/<phase>/sid_report.json
results/<phase>/data_audit.json
results/<phase>/README.md
```

README 必须明确：

- 本阶段改了哪些变量；
- 哪些变量保持不变；
- 数据是否 clean；
- 提升是否超过 seed noise；
- 是否采用为下一阶段默认方案；
- 如果没有提升，为什么失败。

---

## 24. 停止条件

出现以下情况时，必须停止继续堆叠模型：

### 数据层

- metadata coverage 低于 90%，且有大量 ASIN fallback；
- collaborative feature 仍使用 test/valid；
- cold-start 定义仍覆盖绝大多数 item。

### baseline 层

- baseline 与原论文训练协议差距不可解释；
- standard beam 仍无法生成 conflict token；
- baseline 与候选模型容量、beam、SID 长度不一致。

### 方法层

- random/shuffled user context 与真实 context 无差异；
- 提升只来自 beam、参数量、SID 长度或 epoch；
- global assignment 只降低 jump/collision，不提升推荐；
- 联合训练不稳定或无法保持 SID lookup；
- 提升只在一个 seed 或一个分组出现。

---

## 25. 最终推荐研究故事

如果后续 Agent 只能选择一个主线，建议使用下面这个：

### 研究问题

> 在不增加 SID 长度、不扩大 item vocabulary、不增加 decode budget 的条件下，如何利用训练期用户行为信息进行全局 Semantic ID 分配，并通过动态用户上下文条件化生成，提升生成式推荐？

### 三项核心贡献

1. **Behavior-aware global SID assignment**  
   将 SID 分配从局部 suffix/greedy 改为全局目标优化。

2. **User-conditioned generation**  
   使用长期偏好和短期意图的软 token 条件化生成，而不是简单拼接或 hashed user token。

3. **Ranking-aware joint objective**  
   让 tokenizer 同时优化重建、行为对齐和下游排序，而不是只优化重建误差。

### 最小可行创新

```text
标准 TIGER baseline
  + train-only behavior profile
  + behavior-aware global SID assignment
  + user soft-prefix generation
  + ranking-aware objective
```

### 最能成立的论文表述

> We propose a behavior-aware global Semantic ID assignment and user-conditioned generation framework that improves generative recommendation without increasing Semantic ID length, vocabulary size, or decoding budget.

中文表述：

> 本文提出一种行为感知的全局 Semantic ID 分配与用户条件化生成框架，在不增加 Semantic ID 长度、词表规模和解码预算的前提下，提高生成式推荐的准确性与语义保真度。

这应当作为后续代码修改的最终目标。

---

# 26. 执行版总规范：最终创新主线和固定参数

## 26.1 最终创新名称

推荐的论文级主线名称：

```text
Behavior-Cluster-Aware Global Semantic ID Assignment
+ User-Conditioned Generative Retrieval
```

中文：

```text
行为簇感知的全局 Semantic ID 分配
+ 用户条件化生成式推荐
```

简称：

```text
BC-GSID + UCG
```

## 26.2 研究问题

```text
在不增加 SID 长度、不扩大 item vocabulary、不增加 item 数量、
不增加 T5 参数量、不增加 beam 预算的前提下：
如何利用训练期用户行为信息进行全局 SID 分配，
并用动态用户上下文条件化 SID 生成，
从而同时提高 Semantic ID 的语义保真度、
行为一致性、码本均衡性和下一物品推荐性能？
```

## 26.3 三个核心模块

```text
模块 A：Behavior-Cluster-Aware Global SID Assignment
模块 B：User-Conditioned Generation
模块 C：Ranking-Aware Joint Fine-Tuning
```

模块 A 是主创新。

模块 B 是生成端创新。

模块 C 是增强和统一优化，默认放在最后实现。

## 26.4 不再作为主创新的内容

以下只能作为 baseline、消融或工程条件：

- hashed user token；
- 第 4 个 collision token；
- beam20；
- 4×256；
- latent4；
- 最小冲突词表；
- 单纯 gate fusion；
- 单纯 inverse-frequency loss；
- 单纯 hard usage；
- 单纯 reset 周期调整；
- 用户向量直接 concat；
- user bucket 512/2000 的差异。

## 26.5 所有实验的固定协议

```text
数据切分：leave-one-out
验证方式：validation 选择，test 只在最后调用
SID 长度：4
语义层：3
codebook：每层 256
用户 token：2000 buckets，所有主实验固定开启
beam：所有对比统一，默认 20
模型参数量：所有对比一致
训练 epochs/steps：所有对比一致
评测：full-catalog Recall/NDCG + beam 结果报告
随机种子：42, 43, 44；最终确认扩展到 45, 46
```

---

# 27. 数据执行规范

## 27.1 推荐数据量

主实验优先使用：

```text
Amazon Reviews 2014 Beauty 5-core
```

原因：

- 与原论文一致；
- 有 22,363 用户、12,101 物品；
- 现有 GitHub 复现可用；
- 便于与论文和公开实现对齐。

补充实验：

```text
Amazon Reviews 2014 Sports 或 Toys
Amazon Reviews 2023 Books 5-core
```

quick-data 只能用于代码冒烟，不作为最终结论。

## 27.2 随机抽样规则

如果必须使用 quick-data，禁止读取前 N 条。

具体设置：

```text
sampling_mode = stable_hash
sampling_seed = 42
sampling_ratio = 0.05
hash_function = blake2b
hash_input = user_id + "|" + item_id + "|" + timestamp
```

抽样后：

```text
所有 split 仍从抽样数据中重新构造
不能复用旧 quick artifacts
```

## 27.3 metadata 规则

必须记录：

```text
missing_metadata_rate
missing_title_rate
missing_description_rate
missing_category_rate
asin_fallback_rate
```

执行规则：

```text
metadata_coverage >= 0.95：
  可以使用 content SID

0.80 <= metadata_coverage < 0.95：
  必须运行 missing_metadata_ablation

metadata_coverage < 0.80：
  停止 semantic-ID 主实验
  先把数据修复到 >= 0.80
```

默认主实验配置：

```json
{
  "data": {
    "metadata_mode": "exclude_missing",
    "allow_asin_fallback": false,
    "min_metadata_coverage": 0.9
  }
}
```

如果不能排除 missing item：

```json
{
  "data": {
    "metadata_mode": "learned_missing_token",
    "missing_metadata_token": "[MISSING]",
    "allow_asin_fallback": false
  }
}
```

禁止：

```text
直接用 item_id/ASIN 替代空 metadata 后不报告。
```

## 27.4 collaborative 特征规则

必须只使用训练集：

```json
{
  "collaborative": {
    "interaction_source": "train_only",
    "svd_components": 64,
    "normalize": true,
    "random_state": 42,
    "forbid_valid_interactions": true,
    "forbid_test_interactions": true
  }
}
```

如果缺少 train-only features：

```text
直接报错，不允许回退到 interactions.parquet。
```

## 27.5 用户行为簇数据

从训练集构造 user-item matrix。

具体设置：

```text
user embedding：TruncatedSVD 64 维
KMeans 聚类数：32
KMeans n_init：20
KMeans random_state：42
最小簇大小：20
```

如果用户数少于 2,000：

```text
KMeans 聚类数改为 16
```

对于每个 item 计算行为簇分布：

```text
q_i[c] = count(item i 被 cluster c 用户交互) / count(item i 的所有训练交互)
```

平滑：

```text
q_i = (count + alpha) / (total + alpha * K)
alpha = 1.0
```

无训练交互 item：

```text
q_i = null
behavior loss = 0
只使用 reconstruction + semantic 项
```

## 27.6 冻结顺序

```text
1. 读取 review + metadata
2. 随机抽样
3. 去重
4. k-core
5. 构造 train/valid/test
6. 只读 train interactions 构造 collaborative embedding
7. 只读 train interactions 构造 user cluster 和 q_i
8. 训练 RQ-VAE
9. 生成候选 SID
10. 执行全局 SID assignment
11. 冻结 semantic_ids.json
12. 构造 generator 训练数据
13. 训练 generator
14. 最后运行 valid 和 test
```


---

# 28. 标准 TIGER baseline 精确设置

## 28.1 RQ-VAE

```json
{
  "sid": {
    "num_semantic_levels": 3,
    "codebook_size": 256,
    "latent_dim": 32,
    "hidden_dim": 512,
    "dropout": 0.1,
    "epochs": 20000,
    "batch_size": 1024,
    "optimizer": "adagrad",
    "learning_rate": 0.4,
    "weight_decay": 0.0,
    "commitment_weight": 0.25,
    "codebook_weight": 0.25,
    "kmeans_init": true,
    "kmeans_max_iter": 100,
    "target_utilization": 0.8,
    "usage_check_start_epoch": 1000,
    "length_mode": "fixed",
    "collision_mode": "suffix",
    "assignment_mode": "greedy_suffix"
  }
}
```

训练要求：

- 每层记录 utilization；
- 每层记录 perplexity；
- 每层记录 dead codes；
- 记录重建误差；
- 每 100 epochs 记录一次；
- 若利用率达到 0.8 且重建误差不再下降，可以提前停止，但必须记录停止依据。

## 28.2 Generator

```json
{
  "generator": {
    "architecture": "t5_reference",
    "d_model": 128,
    "d_ff": 1024,
    "num_encoder_layers": 4,
    "num_decoder_layers": 4,
    "num_heads": 6,
    "d_kv": 64,
    "dropout": 0.1,
    "max_history_items": 20,
    "max_input_tokens": 256,
    "max_target_tokens": 5,
    "use_time_buckets": true,
    "num_time_buckets": 16,
    "use_behavior_tokens": true,
    "use_user_tokens": true,
    "num_user_buckets": 2000,
    "epochs_or_steps": 200000,
    "batch_size": 256,
    "learning_rate": 0.01,
    "warmup_steps": 10000,
    "scheduler": "inverse_sqrt",
    "gradient_clip": 1.0,
    "decoder": "beam",
    "num_beams": 20,
    "length_penalty": 1.0
  }
}
```

## 28.3 解码

所有对比固定：

```text
num_beams = 20
length_penalty = 1.0
max_decode_steps = 4
```

标准 beam：

- 允许所有 semantic code token；
- 允许 conflict suffix token；
- 允许 EOS；
- 生成后过滤非法 SID；
- 过滤已交互 item；
- 去重。

trie beam：

- 单独报告；
- 不能只报告 trie beam；
- 不能把 trie 的 0% invalid 当作主创新。

## 28.4 评测

必须输出：

```text
Recall@5/10/20
NDCG@5/10/20
MRR@10/20
Coverage
Invalid SID rate
Mean/P95/P99 latency
```

同时输出：

```text
命中人数
精确 95% CI
每 seed 指标
均值与标准差
```

---

# 29. 模块 A：Behavior-Cluster-Aware Global SID Assignment

## 29.1 输入

对每个 item：

```text
item_id
content_latent z_i
behavior_profile b_i
behavior_cluster_distribution q_i
train_count_i
original_rqvae_codes s_i^0
top-k code candidates
codebook embeddings
```

## 29.2 候选 SID 定义

固定长度：

```text
SID = (c0, c1, c2, suffix)
```

其中：

- `c0, c1, c2` 是 3 个 semantic levels；
- `suffix` 是唯一化 token；
- 总长度仍为 4。

禁止通过增加到 4 个 semantic levels 来降低 collision。

## 29.3 候选生成参数

```json
{
  "candidate_generation": {
    "top_codes_per_level": 8,
    "base_candidates": 16,
    "original_code_always_included": true,
    "suffix_candidates_per_original_code": 64,
    "candidate_sort": "reconstruction_error"
  }
}
```

对每个 item 生成：

```text
1. 每个 level 保留 top-8 码字
2. 构造最多 16 个 base code 候选
3. 原 RQ-VAE code 必须保留
4. 对原 code 添加 suffix 0..63
5. 对每个 base candidate 添加 suffix 0
```

候选总数量控制在 81 以内。

## 29.4 行为簇预测器

定义小模型：

```text
h(s) = softmax(W * concat(e_c0, e_c1, e_c2, e_suffix) + b)
```

参数：

```text
input_dim = 4 * latent_dim
hidden_dim = 128
output_dim = num_user_clusters
dropout = 0.1
optimizer = AdamW
lr = 1e-3
weight_decay = 1e-5
epochs = 200
batch_size = 512
loss = cross_entropy(q_i, h(s))
```

只使用训练集 item 的 q_i 训练。

如果 q_i=null：

```text
不参与 h(s) 训练
```

## 29.5 候选 SID 成本函数

对 item i 和候选 SID s：

```text
E(i, s) =
      recon(i, s)
    + lambda_semantic * semantic_drift(i, s)
    + lambda_behavior * behavior_drift(i, s)
    + lambda_cluster  * cluster_drift(i, s)
    + lambda_usage    * usage_penalty(s)
    + lambda_pop      * popularity_penalty(i, s)
```

定义：

```text
recon(i,s) = || z_i - sum_l e_{l, s_l} ||_2^2

semantic_drift(i,s) =
    1 - cosine(z_i, sum_l e_{l, s_l})

behavior_drift(i,s) =
    1 - cosine(b_i, project(concat_l e_{l,s_l}))

cluster_drift(i,s) =
    KL(q_i || h(s))

usage_penalty(s) =
    sum_l max(0, load_l(s_l) - target_load_l)^2 / K

popularity_penalty(i,s) =
    log(1 + train_count_i)
```

默认参数：

```json
{
  "assignment_cost": {
    "lambda_recon": 1.0,
    "lambda_semantic": 0.2,
    "lambda_behavior": 0.1,
    "lambda_cluster": 0.1,
    "lambda_usage": 0.02,
    "lambda_pop": 0.0,
    "target_usage_ratio": 0.8
  }
}
```

第一轮必须令 lambda_pop = 0，防止行为分配退化为 popularity 重排。

## 29.6 求解器

默认求解器：

```text
min_cost_flow
```

图结构：

```text
source -> item node           capacity 1, cost 0
item node -> candidate node   capacity 1, cost E(i,s)
candidate SID -> sink         capacity 1, cost 0
```

约束：

- 每个 item 只能选择一个 SID；
- 每个 SID 最多被一个 item 使用；
- 保证结果唯一；
- 候选不够时动态增加 suffix 候选。

小规模 fallback：

```text
greedy_behavior_aware
```

排序规则：

```text
按 train_count 从高到低处理 item
  -> 对候选 SID 按 E(i,s) 排序
  -> 选择第一个未占用 SID
```

该模式只用于调试，不作为最终论文主结果。

大规模 fallback：

```text
hierarchical_min_cost_flow
```

按第一层码字分组，对每组独立求解。

## 29.7 必须保存的 SID 报告

```json
{
  "assignment_mode": "min_cost_flow",
  "items": 0,
  "sid_length": 4,
  "unique_sids": 0,
  "raw_collision_rate": 0.0,
  "final_collision_rate": 0.0,
  "mean_semantic_drift": 0.0,
  "mean_behavior_drift": 0.0,
  "mean_cluster_kl": 0.0,
  "codebook_utilization": [],
  "codebook_perplexity": [],
  "solver_time_seconds": 0.0
}
```

## 29.8 模块 A 的最小成功标准

必须同时满足：

```text
SID 长度 = 4
item vocabulary 不扩大
模型参数量不增加
final collision = 0
semantic drift <= greedy 的 105%
Recall@20 相对 baseline 提升 >= 2%
提升超过 3 个 seed 的噪声
shuffled behavior profile 无提升
```

否则：

- 不允许把模块 A 作为主创新；
- 只能称为 SID 重构或工程优化。


---

# 30. 模块 B：User-Conditioned Generation 精确实现

## 30.1 用户上下文输入

每个训练样本只使用 target 之前的数据：

```text
history SID tokens
history ratings
history timestamps
hashed user token
```

窗口：

```json
{
  "history_items": 20,
  "short_window": 5,
  "long_window": 50,
  "max_user_context_tokens": 4
}
```

## 30.2 用户编码器

```text
模型：2-layer Transformer encoder
d_model：128
num_heads：4
d_ff：512
dropout：0.1
positional encoding：learned
```

两个池化输出：

```text
u_long：
  对所有可见历史做 attention pooling

u_short：
  只对最近 5 个 item 做 attention pooling
```

## 30.3 融合到 T5

将 u_long 和 u_short 投影为 4 个 soft tokens：

```text
user_soft_tokens = MLP([u_long, u_short]) -> R^(4 x d_model)
```

输入 embedding：

```text
inputs_embeds =
    concat(
        user_soft_tokens,
        T5_embed(input_ids)
    )
```

对应 attention mask：

```text
mask =
    concat(
        ones(4),
        original_attention_mask
    )
```

要求：

- soft tokens 参与 encoder self-attention；
- soft tokens 不参与 loss target；
- hashed user token 继续保留；
- 不得把 user soft token 当作 item token；
- 不得将 target item 信息放入用户编码器。

## 30.4 配置

```json
{
  "user_context": {
    "enabled": true,
    "fusion": "soft_prefix",
    "d_model": 128,
    "num_layers": 2,
    "num_heads": 4,
    "d_ff": 512,
    "dropout": 0.1,
    "short_window": 5,
    "long_window": 50,
    "num_soft_tokens": 4,
    "use_hashed_user_token": true,
    "randomize_for_control": false,
    "shuffle_for_control": false
  }
}
```

## 30.5 训练目标

```text
L_user_gen =
    CE(next SID tokens)
  + lambda_align * InfoNCE(user_context, target_item)
```

默认：

```json
{
  "lambda_align": 0.1,
  "temperature": 0.07,
  "negative_sampling": "in_batch",
  "batch_size": 256
}
```

## 30.6 负向对照

必须实现并运行：

```text
1. use_user_context = false
2. hashed_user_token only
3. random user vector
4. shuffled user vector
5. mean user vector
6. true u_long + u_short
```

评估时按历史长度分组：

```text
history_length <= 5
6 <= history_length <= 20
history_length > 20
```

## 30.7 模块 B 成功标准

```text
相对 hashed user token baseline：
  Recall@20 提升 >= 2%
  NDCG@10 提升 >= 2%
  3 个 seed 均值方向一致
  shuffled user vector 不提升
  短历史用户至少提升 3%
```

否则：

```text
user_context.enabled = false
不得把用户向量融合写入主创新。
```

---

# 31. 模块 C：Ranking-Aware Joint Fine-Tuning

## 31.1 默认状态

```text
默认关闭
只有模块 A 和 B 均通过验收后再开启
```

## 31.2 训练顺序

```text
Stage 0：训练标准 RQ-VAE
Stage 1：执行 BC-GSID 分配并冻结 SID
Stage 2：训练 User-Conditioned Generator
Stage 3：解冻部分 RQ-VAE encoder，联合微调
```

## 31.3 微调设置

```json
{
  "joint_training": {
    "enabled": true,
    "warmup_rqvae_epochs": 20000,
    "warmup_generator_steps": 200000,
    "joint_steps": 20000,
    "freeze_codebooks": true,
    "freeze_decoder_embeddings": false,
    "optimizer": "adamw",
    "generator_lr": 0.0001,
    "rqvae_lr": 0.0001,
    "gradient_clip": 1.0,
    "lambda_recon": 1.0,
    "lambda_commitment": 0.25,
    "lambda_usage": 0.01,
    "lambda_next_sid": 1.0,
    "lambda_ranking": 0.1,
    "lambda_alignment": 0.1,
    "lambda_prefix": 0.05,
    "quantizer": "straight_through",
    "temperature_start": 1.0,
    "temperature_end": 0.1
  }
}
```

## 31.4 排序损失

对每个用户构造：

```text
positive SID：真实 target item
negatives：
  1. 同 batch 其他 target
  2. 同历史最相近但错误的 SID
  3. 高频错误 SID
```

损失：

```text
L_ranking =
    -log exp(score_positive)
         / sum(exp(score_positive) + sum exp(score_negative))
```

默认：

```text
negatives_per_sample = 32
temperature = 0.07
```

## 31.5 前缀一致性损失

```text
L_prefix =
    sum_l KL(
        P(prefix_l | context)
        ||
        stopgrad(P(prefix_{l+1} | context))
    )
```

只在每个 level 的 hidden representation 上计算，不改变 SID token 长度。

## 31.6 模块 C 成功标准

```text
相对冻结 SID 的模块 A+B：
  Recall@20 提升 >= 1.5%
  NDCG@10 提升 >= 1.5%
  semantic drift 不增加 > 5%
  码本利用率不低于 0.8
  SID lookup 仍唯一
```

否则：

```text
joint_training.enabled = false
保留模块 A+B 作为最终方案。
```

---

# 32. 详细配置模板

## 32.1 标准 baseline

```json
{
  "experiment_name": "standard_tiger",
  "data": {
    "dataset": "amazon_beauty_2014",
    "sampling_mode": "full",
    "metadata_mode": "exclude_missing",
    "allow_asin_fallback": false
  },
  "sid": {
    "num_semantic_levels": 3,
    "codebook_size": 256,
    "latent_dim": 32,
    "length_mode": "fixed",
    "collision_mode": "suffix",
    "assignment_mode": "greedy_suffix"
  },
  "generator": {
    "use_user_tokens": true,
    "num_user_buckets": 2000,
    "use_user_context": false,
    "num_beams": 20
  }
}
```

## 32.2 模块 A

```json
{
  "experiment_name": "behavior_cluster_global_sid",
  "data": {
    "dataset": "amazon_beauty_2014",
    "sampling_mode": "full",
    "metadata_mode": "exclude_missing"
  },
  "sid": {
    "num_semantic_levels": 3,
    "codebook_size": 256,
    "latent_dim": 32,
    "length_mode": "fixed",
    "assignment_mode": "behavior_cluster_global"
  },
  "sid_allocation": {
    "solver": "min_cost_flow",
    "top_codes_per_level": 8,
    "base_candidates": 16,
    "suffix_candidates_per_original_code": 64,
    "lambda_recon": 1.0,
    "lambda_semantic": 0.2,
    "lambda_behavior": 0.1,
    "lambda_cluster": 0.1,
    "lambda_usage": 0.02,
    "lambda_pop": 0.0,
    "num_user_clusters": 32
  },
  "generator": {
    "use_user_tokens": true,
    "use_user_context": false,
    "num_beams": 20
  }
}
```

## 32.3 模块 A+B

```json
{
  "experiment_name": "bcgsid_user_conditioned",
  "sid_allocation": {
    "solver": "min_cost_flow",
    "assignment_mode": "behavior_cluster_global"
  },
  "user_context": {
    "enabled": true,
    "fusion": "soft_prefix",
    "num_layers": 2,
    "num_heads": 4,
    "d_model": 128,
    "d_ff": 512,
    "short_window": 5,
    "long_window": 50,
    "num_soft_tokens": 4,
    "use_hashed_user_token": true
  },
  "generator": {
    "use_user_tokens": true,
    "num_user_buckets": 2000,
    "num_beams": 20
  }
}
```

## 32.4 模块 A+B+C

```json
{
  "experiment_name": "bcgsid_user_conditioned_joint",
  "sid_allocation": {
    "solver": "min_cost_flow",
    "assignment_mode": "behavior_cluster_global"
  },
  "user_context": {
    "enabled": true,
    "fusion": "soft_prefix",
    "num_soft_tokens": 4
  },
  "joint_training": {
    "enabled": true,
    "freeze_codebooks": true,
    "joint_steps": 20000,
    "lambda_recon": 1.0,
    "lambda_next_sid": 1.0,
    "lambda_ranking": 0.1,
    "lambda_alignment": 0.1,
    "lambda_prefix": 0.05
  }
}
```

---

# 33. 参数网格与选择规则

## 33.1 第一轮固定默认参数

除非单独做超参数实验，Agent 不得随意修改：

```text
num_user_clusters = 32
top_codes_per_level = 8
base_candidates = 16
suffix_candidates_per_original_code = 64
lambda_recon = 1.0
lambda_semantic = 0.2
lambda_behavior = 0.1
lambda_cluster = 0.1
lambda_usage = 0.02
lambda_pop = 0.0
beam = 20
```

## 33.2 只在默认方案失败时搜索

| 参数 | 搜索范围 | 选择依据 |
|---|---|---|
| num_user_clusters | 16, 32, 64 | validation Recall@20 |
| top_codes_per_level | 4, 8, 16 | recall + semantic drift |
| lambda_behavior | 0.05, 0.1, 0.2, 0.5 | validation Recall@20 |
| lambda_cluster | 0.05, 0.1, 0.2, 0.5 | validation Recall@20 |
| lambda_semantic | 0.1, 0.2, 0.5, 1.0 | semantic drift |
| lambda_usage | 0.0, 0.01, 0.02, 0.05 | utilization |
| num_soft_tokens | 1, 2, 4, 8 | validation NDCG@10 |

选择顺序：

```text
1. validation Recall@20 高者优先
2. Recall 相同则 validation NDCG@10 高者优先
3. 仍相同则 semantic drift 低者优先
4. 仍相同则 latency 低者优先
```

禁止：

```text
用 test 选择参数
只保留最佳 seed
用不同 beam 比较不同方法
```


---

# 34. 代码修改清单：具体文件和函数

> 本节是 Agent 修改代码时的直接执行清单。新增字段必须默认关闭或保持向后兼容。

## 34.1 配置层

文件：

```text
code/tiger_rec/config.py
```

新增 dataclass：

```text
DataAuditConfig
CollaborativeConfig 扩展
SIDAllocationConfig
UserContextConfig
JointTrainingConfig
```

新增字段：

```python
@dataclass
class SIDAllocationConfig:
    enabled: bool = False
    mode: str = "greedy_suffix"
    solver: str = "min_cost_flow"
    top_codes_per_level: int = 8
    base_candidates: int = 16
    suffix_candidates_per_original_code: int = 64
    lambda_recon: float = 1.0
    lambda_semantic: float = 0.2
    lambda_behavior: float = 0.1
    lambda_cluster: float = 0.1
    lambda_usage: float = 0.02
    lambda_pop: float = 0.0
    num_user_clusters: int = 32
    cluster_min_size: int = 20
    random_state: int = 42


@dataclass
class UserContextConfig:
    enabled: bool = False
    fusion: str = "soft_prefix"
    d_model: int = 128
    num_layers: int = 2
    num_heads: int = 4
    d_ff: int = 512
    dropout: float = 0.1
    short_window: int = 5
    long_window: int = 50
    num_soft_tokens: int = 4
    use_hashed_user_token: bool = True


@dataclass
class JointTrainingConfig:
    enabled: bool = False
    warmup_rqvae_epochs: int = 20000
    warmup_generator_steps: int = 200000
    joint_steps: int = 20000
    freeze_codebooks: bool = True
    generator_lr: float = 1e-4
    rqvae_lr: float = 1e-4
    lambda_recon: float = 1.0
    lambda_commitment: float = 0.25
    lambda_usage: float = 0.01
    lambda_next_sid: float = 1.0
    lambda_ranking: float = 0.1
    lambda_alignment: float = 0.1
    lambda_prefix: float = 0.05
```

必须增加配置兼容测试：

```text
旧 config 不出现 sid_allocation/user_context/joint_training 时，
可以正常加载并由默认值关闭。
```

## 34.2 数据层

文件：

```text
code/tiger_rec/data/preprocess.py
code/tiger_rec/data/dataset.py
```

### preprocess.py

新增：

```python
def stable_hash_sample(rows, ratio, seed):
    ...

def audit_metadata(catalog):
    ...

def create_train_only_dataset(cfg):
    ...
```

要求：

- stable hash 抽样，不读取前 N 条；
- 记录 metadata coverage；
- 不允许 ASIN fallback；
- 输出 train/valid/test 和 metadata audit。

### dataset.py

新增数据字段：

```text
short_term_history_sids
long_term_history_sids
short_term_ratings
long_term_ratings
short_term_timestamps
long_term_timestamps
user_context_mask
```

必须保证：

- 每个样本的 user context 只来自 target 前的历史；
- 构建 valid/test 时不能修改 item SID；
- collate 后返回 user context；
- 支持 random/shuffle control。

## 34.3 特征层

文件：

```text
code/tiger_rec/features/collaborative.py
```

新增：

```python
def build_collaborative_embeddings(
    cfg,
    interaction_source="train_only",
):
    ...

def build_user_clusters(
    train_interactions,
    num_clusters=32,
    min_cluster_size=20,
    random_state=42,
):
    ...

def build_item_cluster_distribution(
    train_interactions,
    user_cluster_ids,
):
    ...
```

检查：

```text
build_collaborative_embeddings 读取 valid/test 时必须抛出异常。
```

## 34.4 SID 分配层

新增文件：

```text
code/tiger_rec/semantic_id/assignment.py
code/tiger_rec/semantic_id/behavior_profile.py
```

### behavior_profile.py

新增：

```python
@dataclass
class ItemBehaviorProfile:
    item_id: str
    behavior_vector: np.ndarray
    cluster_distribution: np.ndarray | None
    train_count: int


def build_behavior_profiles(...):
    ...


def train_cluster_predictor(...):
    ...
```

### assignment.py

新增：

```python
@dataclass
class SIDCandidate:
    base_codes: tuple[int, int, int]
    suffix_index: int
    reconstruction_error: float
    semantic_drift: float
    behavior_drift: float
    cluster_kl: float
    usage_penalty: float
    total_cost: float


def generate_candidates(...):
    ...


def score_candidates(...):
    ...


def solve_global_assignment(...):
    ...


def assign_behavior_cluster_global(...):
    ...
```

求解器实现优先级：

```text
1. greedy_behavior_aware：单元测试和 smoke
2. min_cost_flow：主实验
3. hierarchical_min_cost_flow：大规模数据
```

## 34.5 SID 构建层

文件：

```text
code/tiger_rec/semantic_id/build.py
```

修改流程：

```text
encode_all
-> build_behavior_profiles
-> generate_candidates
-> score_candidates
-> solve_global_assignment
-> write semantic_ids.json
-> report diagnostic metrics
```

新增 report 字段：

```text
assignment_mode
semantic_drift
behavior_drift
cluster_kl
codebook load variance
solver time
shuffle_control_score
```

## 34.6 生成器层

新增文件：

```text
code/tiger_rec/models/user_context.py
```

新增：

```python
class UserContextEncoder(nn.Module):
    def forward(
        self,
        short_sid_tokens,
        long_sid_tokens,
        short_ratings,
        long_ratings,
        short_timestamps,
        long_timestamps,
        short_mask,
        long_mask,
    ) -> tuple[Tensor, Tensor]:
        ...
```

修改文件：

```text
code/tiger_rec/models/generator.py
```

新增：

```python
class UserConditionedT5(nn.Module):
    def forward(...):
        ...
```

要求：

- 支持 soft prefix；
- 保留 hashed user token；
- 支持 random user context；
- 支持 shuffled user context；
- 支持关闭 user context；
- 保持原 T5 checkpoint 可加载。

## 34.7 训练层

新增文件：

```text
code/tiger_rec/training/joint.py
```

新增：

```python
def train_joint(
    rqvae,
    generator,
    user_context_encoder,
    train_loader,
    valid_loader,
    cfg,
):
    ...
```

必须支持：

- freeze codebooks；
- gradient clipping；
- loss logging；
- checkpoint rollback；
- SID 唯一性检查；
- 训练中断后恢复。

## 34.8 解码层

文件：

```text
code/tiger_rec/decoding/constrained_beam.py
```

新增参数：

```text
user_context
user_score_weight
popularity_penalty
diversity_penalty
```

保持原有接口兼容：

```python
beam_search(..., user_context=None, user_score_weight=0.0)
```

## 34.9 评测层

文件：

```text
code/tiger_rec/evaluation/metrics.py
code/tiger_rec/evaluation/protocol.py
```

新增：

```python
def bootstrap_ci(hits, n, num_samples=10000, seed=42):
    ...

def group_metrics_by_history_length(...):
    ...

def semantic_drift_metric(...):
    ...

def prefix_purity_metric(...):
    ...

def cluster_alignment_metric(...):
    ...
```

输出：

```text
metrics.json
metrics_by_seed.json
bootstrap_ci.json
group_metrics.json
sid_fidelity.json
```

## 34.10 测试

新增：

```text
code/tests/test_no_leakage.py
code/tests/test_stable_sampling.py
code/tests/test_behavior_profiles.py
code/tests/test_sid_assignment.py
code/tests/test_user_context.py
code/tests/test_metrics_groups.py
code/tests/test_joint_training.py
```

验收顺序：

```text
pytest -> smoke -> validation -> test
```

---

# 35. 实验执行顺序和命令

以下命令是 Agent 实现功能后应提供的标准 CLI。

## 35.1 数据审计

```bash
python code/scripts/prepare_data.py   --config configs/experiments/standard_tiger.json
```

输出：

```text
data/audit/data_sampling_report.json
data/audit/metadata_coverage.json
data/audit/train_test_stats.json
```

## 35.2 标准 baseline

```bash
python code/scripts/encode_items.py   --config configs/experiments/standard_tiger.json

python code/scripts/build_semantic_ids.py   --config configs/experiments/standard_tiger.json   --assignment-mode greedy_suffix

python code/scripts/train_generator.py   --config configs/experiments/standard_tiger.json

python code/scripts/evaluate.py   --config configs/experiments/standard_tiger.json   --method tiger_standard

python code/scripts/evaluate.py   --config configs/experiments/standard_tiger.json   --method tiger_trie
```

## 35.3 模块 A

```bash
python code/scripts/build_semantic_ids.py   --config configs/experiments/behavior_cluster_global_sid.json   --assignment-mode behavior_cluster_global

python code/scripts/train_generator.py   --config configs/experiments/behavior_cluster_global_sid.json

python code/scripts/evaluate.py   --config configs/experiments/behavior_cluster_global_sid.json   --method tiger_standard
```

## 35.4 模块 A+B

```bash
python code/scripts/train_generator.py   --config configs/experiments/bcgsid_user_conditioned.json

python code/scripts/evaluate.py   --config configs/experiments/bcgsid_user_conditioned.json   --method tiger_standard

python code/scripts/evaluate.py   --config configs/experiments/bcgsid_user_conditioned.json   --method tiger_trie
```

## 35.5 模块 A+B+C

```bash
python code/scripts/train_generator.py   --config configs/experiments/bcgsid_user_conditioned_joint.json   --joint-training
```

## 35.6 逐项消融

```bash
python code/scripts/run_ablations.py   --base-config configs/experiments/bcgsid_user_conditioned.json   --matrix configs/ablations/innovation_matrix.yaml
```

必须支持：

```text
behavior=off/on
user_context=off/on
assignment=greedy/global
fusion=none/concat/soft_prefix/cross_attention
joint=off/on
```

---

# 36. 评测、置信区间和验收规则

## 36.1 每个实验必须输出

```json
{
  "seed": 42,
  "config": "bcgsid_user_conditioned",
  "recall@5": 0.0,
  "recall@10": 0.0,
  "recall@20": 0.0,
  "ndcg@5": 0.0,
  "ndcg@10": 0.0,
  "ndcg@20": 0.0,
  "mrr@20": 0.0,
  "coverage": 0.0,
  "invalid_sid_rate": 0.0,
  "hits": 0,
  "users": 0,
  "ci95_low": 0.0,
  "ci95_high": 0.0,
  "p99_ms": 0.0,
  "mean_ms": 0.0
}
```

## 36.2 统计选择

默认使用 3 个 seed：

```text
42, 43, 44
```

只有满足以下条件才扩展到 5 个 seed：

```text
3 seed 均值提升 >= 2%
3 seed 方向一致
p < 0.05
```

## 36.3 主结果判定

模块 A 或 A+B 只有满足以下全部条件，才能作为主创新：

```text
1. 相同 SID 长度
2. 相同 item vocabulary
3. 相同模型参数量
4. 相同 beam
5. validation 选择参数
6. test 不参与选择
7. Recall@20 相对提升 >= 2%
8. NDCG@10 相对提升 >= 2%
9. 提升超过 seed noise
10. 随机和打乱控制均不提升
11. semantic drift 不恶化超过 5%
12. 至少两个数据规模成立
```

## 36.4 负结果处理

如果模块 A 无效：

```text
保留行为簇分析和 SID 报告
关闭 behavior/cluster loss
不得写成创新
进入模块 C
```

如果模块 B 无效：

```text
关闭 user_context
只保留 hashed user token
不得把用户向量拼接写成创新
```

如果模块 C 无效：

```text
关闭 joint_training
使用模块 A+B 中有效的部分
```


---

# 37. 实施阶段和提交边界

## 37.1 Phase 0 提交

必须只包含：

- 随机抽样；
- metadata audit；
- train-only collaborative source；
- cold-start 定义；
- 对应测试。

不得包含：

- gate/user/behavior SID；
- beam 调参；
- 新模型结构。

## 37.2 Phase 1 提交

必须只包含：

- 标准 RQ-VAE；
- 3 semantic levels；
- 第 4 个唯一化 token；
- hashed user token；
- 标准 beam；
- baseline metrics。

不得包含：

- 4 个 semantic levels；
- user vector；
- behavior cluster；
- joint training。

## 37.3 Phase 2 提交

必须只包含：

- user context encoder；
- soft prefix；
- random/shuffle controls；
- history-length 分组。

## 37.4 Phase 3 提交

必须只包含：

- behavior profile；
- candidate generation；
- cost function；
- greedy 和 min-cost-flow；
- SID allocation report。

## 37.5 Phase 4 提交

必须只包含：

- user/cluster conditioned allocation；
- 与 Phase 3 的单变量比较；
- 所有负向控制。

## 37.6 Phase 5 提交

必须只包含：

- joint training；
- ranking loss；
- prefix consistency；
- 稳定性测试。

## 37.7 Phase 6 提交

必须只包含：

- 多数据集；
- 多 seed；
- 统计检验；
- 最终论文级结果。

每个 Phase 一个独立分支：

```text
codex/phase0-data-audit
codex/phase1-standard-tiger
codex/phase2-user-context
codex/phase3-behavior-sid
codex/phase4-conditional-sid
codex/phase5-joint-training
codex/phase6-final-eval
```

---

# 38. 必须写入最终文档的实验结论

当且仅当所有验收门通过时，才允许写：

```text
我们提出 Behavior-Cluster-Aware Global SID Assignment
和 User-Conditioned Generation。
在不增加 SID 长度、词表规模、模型参数量和解码预算的情况下，
相对标准 TIGER baseline 在 Recall@20 和 NDCG@10 上取得稳定提升。
```

如果没有通过，只能写：

```text
我们系统分析了 TIGER Semantic ID 分配和用户条件化生成，
并在受控协议下报告了正负结果。
```

禁止写法：

```text
用户向量拼接带来了创新。
beam 增大带来了优化。
4 层 RQ-VAE 带来了创新。
SID 冲突从 X 降到 0 就等于推荐提升。
```

---

# 39. 最终验收清单

## 数据

- [ ] 不再读取前 N 条；
- [ ] metadata coverage >= 0.9；
- [ ] collaborative 只用 train；
- [ ] valid/test 不进入 SID 构建；
- [ ] cold-start 定义与 long-tail 分离。

## Baseline

- [ ] 3 semantic levels；
- [ ] 第 4 个唯一化 token；
- [ ] hashed user token；
- [ ] 标准 beam 允许 conflict token；
- [ ] trie beam 单独报告；
- [ ] 相同 beam 和参数量。

## 模块 A

- [ ] SID 长度仍为 4；
- [ ] 不扩大 codebook；
- [ ] behavior profile 只用 train；
- [ ] min-cost-flow 结果唯一；
- [ ] semantic drift 不恶化；
- [ ] 成功标准通过。

## 模块 B

- [ ] user context 只使用 prefix history；
- [ ] random/shuffle control 已运行；
- [ ] 短历史用户分析已完成；
- [ ] 成功标准通过。

## 模块 C

- [ ] codebook 冻结；
- [ ] 排序损失稳定；
- [ ] SID lookup 唯一；
- [ ] 成功标准通过。

## 统计

- [ ] 3 个 seed 起；
- [ ] 5 个 seed 确认；
- [ ] 95% CI；
- [ ] paired bootstrap；
- [ ] test 未被用于选择。

---

# 40. 一句话执行原则

后续 Agent 只需要按以下顺序工作：

```text
先修数据
-> 再对齐标准 TIGER
-> 再做用户条件化生成
-> 再做行为簇感知的全局 SID 分配
-> 最后做联合训练
```

最终必须保持：

```text
SID 长度不变
item vocabulary 不变
模型参数量不变
beam 不变
训练预算不变
```

只有在这种条件下得到稳定提升，才是真正值得写入论文的算法创新。


---

# 41. 优化方向与 baseline 完整对比表

> 本节用于让后续 Agent 在修改代码前明确：谁是 baseline、谁是调参、谁是真正方法改动、每个模块应该和谁比较。

## 41.1 实验代号

| 代号 | 层级 | 含义 | 定位 |
|---|---|---|---|
| B0-current | L0 Legacy | 当前仓库弱 baseline：content-only、3×256/latent16、无 user token、beam5 | 历史结果，不能作为主比较 |
| B1-standard | L1 | 标准 TIGER baseline：3 语义层 + 唯一化 token、latent32、user token、统一 beam | 忠实 baseline |
| B1-tuned | L2 | 仅在 validation 上确定 levels/latent/beam 等超参数的强 baseline | L3 的直接底座 |
| O1-current | L2-engineering | gate + 逆频 + usage + entropy + hard usage + reset | 工程修复和消融，不是 L3 主创新 |
| O2-current | Legacy mixed | O1 + 4 层 + latent4 + all-level reallocation + user token + beam20 | 必须拆分，不能整体比较 |
| I1-BC-GSID | L3 | 行为簇感知全局 SID 分配 | 主创新候选 A |
| I2-UCG | L3 | 用户条件化生成 | 主创新候选 B |
| I3-BC-GSID+UCG | L3 | 模块 A+B 合并 | 主创新主线 |
| I4-Joint | L3 | 加入 ranking-aware joint fine-tuning | L3 增强版 |
| B1-tuned-5token | L2 variant | 当 L2 选择 4 semantic levels + suffix 时形成的固定 5-token tuned baseline | 非标准 TIGER baseline |

## 41.2 数据与实验协议对比

| 维度 | B0-current | B1-standard | O1-current | O2-current | I1-BC-GSID | I2-UCG | I3-BC-GSID+UCG |
|---|---|---|---|---|---|---|---|
| 数据来源 | Amazon 2023 Books 前 100 万条 | Amazon 2014 Beauty 5-core | 同 B0 | 同 B0 | 推荐 Beauty/Sports/Toys 或完整 5-core | 同 I1 数据 | 同 I1 数据 |
| 抽样方式 | 读取前 N 条 | 完整 5-core | 读取前 N 条 | 读取前 N 条 | 随机 hash 或完整数据 | 同 I1 | 同 I1 |
| metadata | 约 75.65% 缺失 | 常规元数据完整 | 同 B0 | 同 B0 | 要求 coverage >= 0.9 | 同 I1 | 同 I1 |
| collaborative 来源 | content-only | 不使用或 train-only | 全量 interactions，含 test/valid | 同 O1 | 只用 train interactions | 只用 train | 只用 train |
| valid/test | 参与 collaborative leakage 风险 | 仅评测 | 参与 leakage 风险 | 参与 leakage 风险 | 不参与 SID 构建 | 只使用 prefix history | 不参与 SID/用户画像构建 |
| cold-start 定义 | train_count <= 3，覆盖 97.82% | 真正 unseen item | 同 B0 | 同 B0 | unseen 与 few-shot 分离 | 同 I1 | 同 I1 |
| 结论可信度 | 低 | 高 | 中低，存在归因和泄漏 | 低，多个变量同时改变 | 目标高 | 目标高 | 目标高 |

## 41.3 SID 与 RQ-VAE 对比

| 维度 | B0-current | B1-standard | O1-current | O2-current | I1-BC-GSID |
|---|---|---|---|---|---|
| 语义层数 | 3 | 3 | 3 | 4 | 3 |
| codebook | 256/层 | 256/层 | 256/层 | 256/层 | 256/层 |
| latent | 16 | 32 | 16 | 4 | 32 |
| SID 长度 | 4 | 4 | 4 | 5 | 4 |
| item vocabulary | 不变 | 不变 | 不变 | 不变 | 不变 |
| 第 4 个 token | suffix 0 | 唯一化 token/prefix token | suffix 0 | suffix 0 | suffix token，唯一化 |
| 基础容量 | 256^3 | 256^3 | 256^3 | 256^4 | 256^3 |
| 训练优化器 | AdamW | Adagrad | AdamW | AdamW | 先 Adagrad 标准训练，再做分配 |
| 学习率 | 5e-4 | 0.4 | 5e-4 | 5e-4 | 0.4 |
| epochs | 100 | 20000 | 100 | 100 | 20000 |
| 码本初始化 | KMeans | KMeans | KMeans | KMeans | KMeans |
| 码本坍塌处理 | 无 | 依赖标准训练 | gate/usage/entropy/hard/reset | 同 O1 | 先标准训练，再全局分配 |
| 冲突处理 | last-level greedy + suffix | suffix 唯一化 | last-level + suffix | all-level reallocation + suffix0 | 全局 min-cost-flow 分配 |
| 行为信息 | 无 | 无 | gate 使用泄漏风险 collab | 同 O1 | train-only behavior profile |
| semantic drift 控制 | 无 | 无 | 无 | 无 | 有显式 semantic_drift 项 |
| behavior drift 控制 | 无 | 无 | 无 | 无 | 有显式 behavior_drift 项 |
| cluster alignment | 无 | 无 | 无 | 无 | 有 KL(q_i || h(s)) |
| 是否真正算法创新 | 否 | baseline | 主要是组合修复 | 主要是容量和调参 | 目标主创新 |

## 41.4 生成器、用户信息与解码对比

| 维度 | B0-current | B1-standard | O1-current | O2-current | I2-UCG |
|---|---|---|---|---|---|
| hashed user token | 无 | 2000 bucket | 无 | 512 bucket | 2000 bucket，保留 |
| 用户向量 | 无 | 无 | 无 | 无 | 长期 + 短期 |
| 用户信息融合 | 无 | user token | 无 | user token | soft prefix 4 tokens |
| 用户编码器 | 无 | 无 | 无 | 无 | 2-layer Transformer，d=128 |
| 长期窗口 | 无 | 无 | 无 | 无 | 50 |
| 短期窗口 | 无 | 无 | 无 | 无 | 5 |
| 负向控制 | 无 | 无 | 无 | 无 | random/shuffle/mean |
| 生成器 | T5-tiny 风格 | T5 约 4.85M–13M | 同 B0 | 同 B0 | 与原生成器容量一致 |
| beam | 5 | 20/统一协议 | 5 | 20 | 20 |
| 排序目标 | CE | CE | CE | CE | CE + InfoNCE |
| 用户条件参与解码 | 无 | 仅 token | 无 | 仅 token | soft prefix + 可选 score |
| 是否可直接归因 | 否 | baseline | 否 | 否 | 必须做 random/shuffle 对照 |

## 41.5 当前优化点与 proposed 创新的对比

| 项目 | O1-current | O2-current | I1-BC-GSID | I2-UCG | I3-BC-GSID+UCG |
|---|---|---|---|---|---|
| 主要改动位置 | RQ-VAE 训练 | SID 几何 + 生成 | SID 分配 | 生成器条件融合 | 分配 + 生成 |
| 是否改变训练目标 | 部分改变 | 否 | 是 | 是 | 是 |
| 是否改变 SID 分配算法 | 否，仍是局部贪心 | 是，启发式局部重排 | 是，全局优化 | 否 | 是 |
| 是否增加容量 | 否 | 是 | 否 | 否 | 否 |
| 是否增加 SID 长度 | 否 | 从 4 到 5 | 否 | 否 | 否 |
| 是否增加 beam | 否 | 是，5 到 20 | 否 | 否 | 否 |
| 是否使用用户信息 | 否 | 原论文 user token | 行为簇分布 | 长期/短期用户向量 | 两者 |
| 可否归因单一机制 | 否 | 否 | 可以做单变量归因 | 可以做单变量归因 | 可以分层归因 |
| 能否称为主创新 | 不能 | 不能 | 有可能 | 有可能 | 最有希望 |
| 先决条件 | 数据修复 | 无 | 标准 baseline | 标准 baseline | A+B 均通过 |

## 41.6 每个模块应该和谁比较

| 实验 | 只允许改变 | 必须固定 |
|---|---|---|
| I1 vs B1-standard | SID assignment 算法 | 数据、SID 长度、codebook、模型、beam |
| I2 vs B1-standard | 用户上下文融合 | 数据、SID、模型容量、beam、user token |
| I3 vs I1 | user context | SID assignment、数据、模型容量 |
| I3 vs I2 | SID assignment | 用户上下文、模型容量、数据 |
| I4 vs I3 | joint training | SID 长度、vocab、数据、beam |
| I4 vs B1-standard | 完整创新包 | 所有预算相同 |

禁止以下比较：

| 错误比较 | 为什么错误 |
|---|---|
| O2 beam20 vs B0 beam5 | 把 decode budget 当创新 |
| O2 4×256 vs B0 3×256 | 把容量扩大当创新 |
| 有 user token vs 无 user token | 原论文已有组件 |
| collab 含 test vs 不含 test | 数据泄漏导致不可比 |
| trie beam vs 无法生成 suffix 的 standard beam | 标准 beam 实现是稻草人 |
| 只报告最佳 seed vs baseline 单 seed | 选择偏差 |

## 41.7 预期收益与失败表现

| 模块 | 预期提升 | 合理有效信号 | 失败信号 |
|---|---|---|---|
| I1-BC-GSID | SID 唯一且 semantic drift 可控 | Recall@20 +2%，behavior shuffle 掉点 | 只降 collision，不提升 Recall |
| I2-UCG | 短历史用户个性化 | 短历史 Recall +3%，shuffle 掉点 | 所有用户同等提升，shuffle 不掉点 |
| I3-BC-GSID+UCG | 分配与生成互补 | 超过 I1、I2 单独结果 | 只等于其中一项 |
| I4-Joint | tokenizer/generator 对齐 | Recall/NDCG 再提升，SID 稳定 | 码本漂移、lookup 不唯一、loss 不稳 |

## 41.8 Agent 的采用规则

按以下规则自动决定是否保留模块：

```text
I1：
  若 Recall@20 提升 >= 2% 且 shuffle 对照有效 -> 保留
  否则 -> 关闭 behavior/cluster loss，只保留 SID 诊断报告

I2：
  若超过 hashed user token >= 2% 且 shuffle/random 对照有效 -> 保留
  否则 -> 关闭 user_context

I3：
  若同时超过 I1 和 I2 -> 作为最终主模型
  否则 -> 只保留有效的单模块

I4：
  若超过 I3 >= 1.5% 且 SID lookup 和码本稳定 -> 保留
  否则 -> 关闭 joint_training
```

## 41.9 最终论文主表应该怎么呈现

最终主表建议使用以下列：

| Method | SID length | Vocab size | Model params | Beam | Recall@10 | Recall@20 | NDCG@10 | Semantic drift | Invalid rate | P99 ms |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| SASRec | — | — | 相同规模 | — | — | — | — | — | — | — |
| Standard TIGER | 4 | 不变 | 不变 | 20 | — | — | — | — | — | — |
| TIGER + user token | 4 | 不变 | 不变 | 20 | — | — | — | — | — | — |
| TIGER + BC-GSID | 4 | 不变 | 不变 | 20 | — | — | — | — | — | — |
| TIGER + UCG | 4 | 不变 | 不变 | 20 | — | — | — | — | — | — |
| BC-GSID + UCG | 4 | 不变 | 不变 | 20 | — | — | — | — | — | — |
| BC-GSID + UCG + Joint | 4 | 不变 | 不变 | 20 | — | — | — | — | — | — |

主表必须让审稿人一眼看出：

```text
SID 长度没有增加
vocabulary size 没有增加
模型参数量没有增加
beam 没有增加
提升来自分配和条件化机制，而不是算力
```

## 41.10 最终结论表

| 判断 | 结论 |
|---|---|
| 当前优化点 1 是不是单一 dead-code revival？ | 不是，同时开启多个组件，不能单一归因 |
| 当前优化点 2 是不是真正创新？ | 不是，主要含 baseline 组件、容量和 beam 调参 |
| 用户向量直接拼接是不是创新？ | 通常不是，只是 fusion baseline |
| 什么才是真正创新？ | 用户/行为条件化 SID 分配、联合训练、解码校准 |
| 最推荐主线是什么？ | BC-GSID + UCG，必要时再加 Joint |
| 第一优先级是什么？ | 修复数据泄漏、metadata 和标准 baseline |
| 最终硬约束是什么？ | SID 长度、vocab、模型参数、beam 和训练预算全部保持一致 |


---

# 42. 三层递进实验体系（最终执行版）

> 本节是后续 Agent 进行数据实验、代码实现和调参时的最高优先级规则。
> 如果前文其他实验顺序、baseline 定义或比较方式与本节冲突，以本节为准。

## 42.1 三层定义

```text
L1：B1-standard
标准 TIGER baseline

L2：B1-tuned
只在 validation 上做超参数选择后的强 baseline

L3：创新方法
BC-GSID
UCG
BC-GSID + UCG
Joint
```

三层递进关系：

```text
L1 B1-standard
        │
        ▼
L2 B1-tuned
        │
        ├── L3-I1：BC-GSID
        ├── L3-I2：UCG
        ├── L3-I3：BC-GSID + UCG
        └── L3-I4：BC-GSID + UCG + Joint
```

## 42.2 三层之间的关系

### L1 是标准复现

L1 只回答一个问题：

```text
在标准 TIGER 配置下，模型在干净数据上能达到什么水平？
```

L1 不允许：

- 临时改变 SID 长度；
- 临时改变 user token；
- 临时改变 beam；
- 临时加入 behavior assignment；
- 临时加入 user context。

### L2 是调参后的强 baseline

L2 只回答另一个问题：

```text
在相同数据、相同模型、相同评测协议下，
标准 TIGER 经过 validation 调参后能达到什么水平？
```

L2 可以调整：

- semantic levels；
- codebook size；
- latent dimension；
- beam width；
- RQ-VAE 学习率；
- T5 学习率；
- history window。

L2 不允许调整：

- SID 分配算法；
- 用户条件融合结构；
- joint training；
- popularity debiasing 作为创新；
- 数据泄漏或额外测试信息。

### L3 是创新方法

L3 必须建立在 L2 之上：

```text
数据 = L2 数据
SID 长度 = L2 SID 长度
vocab = L2 vocab
模型参数量 = L2 参数量
beam = L2 beam
训练预算 = L2 预算
只改变创新机制
```

## 42.3 L1：B1-standard 精确规范

### 配置

```text
数据：完整 5-core 或严格随机抽样
metadata coverage：>= 0.9
collaborative：不使用，或只使用 train-only
semantic levels：3
codebook：256
latent：32
SID：3 semantic tokens + 1 unique token
总长度：4
user token：2000 buckets
beam：20
T5：与论文对齐或明确记录容量差距
```

### 目的

```text
建立忠实 baseline
确认 baseline 没有数据泄漏
确认 baseline 的 codebook 利用率
确认 baseline 的 recall/ndcg
确认 baseline 的 invalid SID rate
```

### 输出

```text
results/L1-standard/metrics.json
results/L1-standard/seed_variance.json
results/L1-standard/sid_report.json
results/L1-standard/data_audit.json
```

## 42.4 L2：B1-tuned 精确规范

### L2 的调参范围

只能在 validation 上搜索：

```text
num_semantic_levels = {3, 4}
codebook_size = {128, 256}
latent_dim = {8, 16, 32}
beam = {5, 10, 20, 50}
rqvae_lr = {1e-4, 5e-4, 1e-3}
generator_lr = {1e-4, 3e-4, 1e-3}
history_window = {10, 20, 50}
```

默认搜索顺序：

```text
1. 先固定 3 levels、latent32、beam20
2. 再搜索 latent
3. 再搜索 levels
4. 再搜索 beam
5. 最后搜索 learning rate
```

不得一次性做全网格搜索。

### L2 的选择规则

```text
第一优先级：validation Recall@20 最高
第二优先级：validation NDCG@10 最高
第三优先级：semantic drift 最低
第四优先级：invalid SID rate 最低
第五优先级：P99 latency 最低
```

### L2 输出

```text
results/L2-tuned/config.json
results/L2-tuned/metrics.json
results/L2-tuned/seed_variance.json
results/L2-tuned/tuning_log.json
results/L2-tuned/selected_hyperparameters.json
```

### L2 的硬约束

```text
test 只能最终评测一次
不能使用 test 选 levels
不能使用 test 选 latent
不能使用 test 选 beam
不能使用 test 选 seed
不能只保留最佳 seed
```

## 42.5 L2 选择 4 levels 或 latent4 时怎么处理

如果 validation 最终选择：

```text
num_semantic_levels = 4
latent_dim = 4
```

则 L2 变为：

```text
B1-tuned-5token
```

这时的定义是：

```text
4 semantic levels
+ 1 collision suffix
总 SID 长度 = 5
```

注意：

- 这不是标准 4-token TIGER baseline；
- 这是固定 5-token 的 tuned baseline；
- 所有 L3 方法必须继承 4 levels、latent4、5-token SID；
- 不能 BC-GSID 用 4 层，而 UCG 用 3 层；
- 不能 baseline 用 beam5，而 L3 用 beam20；
- 不能 baseline 用 latent4，而 L3 用 latent32。

如果最终选择 3 levels：

```text
L2 保持标准 4-token SID
L3 全部继承 4-token SID
```

## 42.6 L3 的四个实验分支

### L3-I1：BC-GSID

只改变：

```text
SID assignment algorithm
```

继承：

```text
L2 数据
L2 RQ-VAE 结构
L2 codebook
L2 SID 长度
L2 用户 token
L2 generator 结构
L2 beam
L2 训练预算
```

需要重新训练 generator，因为 SID 已经变化。

主比较：

```text
L3-I1 vs L2
```

辅助比较：

```text
L3-I1 vs L1
```

### L3-I2：UCG

只改变：

```text
generator 的用户条件融合
```

继承：

```text
L2 数据
L2 SID
L2 RQ-VAE
L2 codebook
L2 item vocabulary
L2 beam
L2 训练预算
```

需要重新训练 generator。

主比较：

```text
L3-I2 vs L2
```

关键对照：

```text
hashed user token only
random user context
shuffled user context
true long-term + short-term context
```

### L3-I3：BC-GSID + UCG

同时改变：

```text
SID assignment
generator user conditioning
```

必须比较：

```text
L3-I3 vs L2
L3-I3 vs L3-I1
L3-I3 vs L3-I2
```

只有同时超过 I1 和 I2，才能说明两个模块互补。

### L3-I4：Joint

只在 L3-I3 通过后执行。

只增加：

```text
ranking-aware joint fine-tuning
prefix consistency
alignment loss
```

主比较：

```text
L3-I4 vs L3-I3
```

辅助比较：

```text
L3-I4 vs L2
```

## 42.7 配置继承矩阵

| 配置项 | L1 | L2 | L3-I1 | L3-I2 | L3-I3 | L3-I4 |
|---|---|---|---|---|---|---|
| 数据 | 标准 | L1 调优后 | L2 | L2 | L2 | L2 |
| metadata | 标准 | L2 | L2 | L2 | L2 | L2 |
| collaborative | 无/train-only | L2 | L2 | L2 | L2 | L2 |
| semantic levels | 3 | validation 决定 | L2 | L2 | L2 | L2 |
| codebook | 256 | validation 决定 | L2 | L2 | L2 | L2 |
| latent | 32 | validation 决定 | L2 | L2 | L2 | L2 |
| SID 长度 | 4 | L2 决定 | L2 | L2 | L2 | L2 |
| user token | 2000 | L2 | L2 | L2 | L2 | L2 |
| SID assignment | suffix | L2 | BC-GSID | L2 | BC-GSID | BC-GSID |
| user context | 无 | L2 | L2 | UCG | UCG | UCG |
| generator 主体 | 标准 | L2 | L2 | L2 + UCG | L2 + UCG | L2 + UCG |
| beam | 20 | validation 决定 | L2 | L2 | L2 | L2 |
| 训练预算 | 标准 | L2 | L2 | L2 | L2 | L2 |
| joint training | 关闭 | 关闭 | 关闭 | 关闭 | 关闭 | 开启 |

## 42.8 训练和重训规则

### L1

```text
从零训练
seed = 42,43,44
```

### L2

```text
从零训练
每个超参数候选使用 validation
最终配置使用 test 一次
seed = 42,43,44
```

### L3-I1

```text
RQ-VAE 权重可以继承 L2
SID 重新分配
generator 必须从零训练或从相同初始化训练
训练步数必须与 L2 一致
```

### L3-I2

```text
SID 继承 L2
generator 加入用户编码器
为了公平，用户编码器参数量要报告
如果参数增加，必须同时提供等参数 baseline
```

### L3-I3

```text
重新构建 SID
重新训练 generator
不能复用 I1/I2 的最终 checkpoint
可以复用相同随机初始化和训练步数
```

### L3-I4

```text
从 I3 checkpoint 开始
保持 codebook 冻结
只微调 generator 和少量 RQ-VAE encoder
增加 ranking、prefix、alignment loss
```

## 42.9 必须运行的实验矩阵

| 编号 | 实验 | 对比对象 | 目的 |
|---|---|---|---|
| E0 | L1-standard | paper / 公开复现 | 验证基础复现 |
| E1 | L2-tuned | L1-standard | 量化调参贡献 |
| E2 | behavior-only SID | L2 | 验证行为分配 |
| E3 | cluster-only SID | L2 | 验证用户簇约束 |
| E4 | BC-GSID | L2 | 验证完整分配方法 |
| E5 | UCG | L2 | 验证用户条件生成 |
| E6 | BC-GSID + UCG | L2、E4、E5 | 验证互补性 |
| E7 | Joint | E6 | 验证联合训练 |
| E8 | random behavior | E4 | 排除偶然 |
| E9 | shuffled user context | E5 | 排除用户记忆 |
| E10 | no user token | L2 | 证明原论文组件必要性 |
| E11 | no collision token | L2 | 检查 SID 唯一性 |
| E12 | equal parameter control | E5 | 排除参数量影响 |

## 42.10 统计协议

所有 L3 主结果必须：

```text
至少 3 seeds：42,43,44
最终确认扩展到 5 seeds：42,43,44,45,46
每个 seed 使用相同数据划分
报告每 seed 指标
报告均值和标准差
报告 95% CI
做 paired bootstrap
test 只用于最终评测
```

主结论阈值：

```text
BC-GSID：Recall@20 相对 L2 >= +2%
UCG：Recall@20 相对 L2 >= +2%
BC-GSID+UCG：相对 L2 >= +3%
Joint：相对 I3 >= +1.5%
```

如果未达到阈值：

```text
模块不进入主创新
只记录为消融或工程优化
```

## 42.11 三层决策树

```text
L1 是否成功？
  否 -> 先修数据、模型或训练协议
  是 -> 进入 L2

L2 是否显著优于 L1？
  否 -> 检查超参数搜索和评估配置
  是 -> 冻结 L2

BC-GSID 是否显著优于 L2？
  否 -> 关闭 behavior/cluster 主创新，保留诊断
  是 -> 保留 L3-I1

UCG 是否显著优于 L2？
  否 -> 关闭 user_context
  是 -> 保留 L3-I2

I1 和 I2 是否互补？
  否 -> 只使用效果更好的单模块
  是 -> 保留 L3-I3

Joint 是否显著优于 I3？
  否 -> 关闭 joint_training
  是 -> 保留 L3-I4
```

## 42.12 最终论文表结构

最终主表必须按三层展示：

```text
L1-standard
L2-tuned
L3-BC-GSID
L3-UCG
L3-BC-GSID+UCG
L3-Joint
```

主表固定列：

```text
SID length
Vocabulary size
Model parameters
Beam
Recall@10
Recall@20
NDCG@10
Semantic drift
Invalid SID rate
P99 latency
```

并在表下写清楚：

```text
L3 与 L2 的数据、SID 长度、词表、模型参数、
训练预算和 beam 完全一致。
提升来自 SID 分配或用户条件化机制。
```

## 42.13 最终原则

```text
L1 负责证明复现可信。
L2 负责消除“创新只是调参”的质疑。
L3 负责提出和验证真正的方法创新。
```

Agent 不得跳过 L2，直接从 L1 跳到 L3。  
Agent 不得把 L2 的调参结果写成 L3 创新。  
Agent 不得让 L3 使用比 L2 更大的模型、更长的 SID 或更大的 beam。  
只有满足上述三条，实验结果才具有论文级可信度。


---

# 43. RTX 5090 一晚折中执行版

> 本节是今晚的唯一执行入口。  
> 目标不是完成论文级复现，而是在一张 RTX 5090 上，用 8–12 小时跑通整条创新链路，得到单 seed 的方向性结论。  
> 今晚以本节的缩减实验为准；论文级结果后续按第 42 节补齐。

## 43.1 今晚目标

今晚必须完成：

~~~text
1. 构建最终约 1M interactions 的高质量 Books 5-core 子集
2. 训练一次 50K item 的 RQ-VAE
3. 构建 L2-lite baseline SID
4. 构建 BC-GSID
5. 训练 L2-lite generator
6. 训练 BC-GSID generator
7. 训练 UCG generator
8. 训练 BC-GSID+UCG generator
9. 从 BC-GSID+UCG 进行 Joint 微调
10. 在 2K–3K users 上完成 beam/trie 评测
11. 输出 baseline 与创新方法的对比表
~~~

今晚不要求：

~~~text
L1 完整标准复现
L2 完整网格调参
3–5 seeds
完整 776K user test
完整 exhaustive ranking
正式论文级置信区间
~~~

今晚结果只能命名为：

~~~text
single-seed RTX 5090 overnight screening
~~~

## 43.2 数据规模

目标：

| 项目 | 今晚目标 |
|---|---:|
| 数据源 | Amazon Reviews 2023 Books 5-core |
| 最终 interactions | 1,000,000 |
| users | 80K–120K |
| items | top-50K |
| valid users | 全部保留用户或抽样 |
| test users | 2K–3K |
| beam | 20 |
| seed | 42 |

### 数据构造步骤

~~~text
1. 读取 Books 5-core
2. 从 776.4K users 中随机抽 120K–150K users
3. 保留这些用户的全部交互
4. 只使用 train split 统计 item 频次
5. 取 train 频次 top-50K items
6. 删除不在 top-50K 内的 item
7. 重新执行 user>=5, item>=5, sequence_length>=5
8. 如果最终 interactions < 1M，继续增加用户采样
9. 最终保留 >= 1M interactions
~~~

必须输出：

~~~text
data/overnight/sample_config.json
data/overnight/user_sampling_stats.json
data/overnight/item_cap_stats.json
data/overnight/final_data_stats.json
data/overnight/split_stats.json
~~~

### item cap 规则

只允许使用 train split 计算 popularity：

~~~text
item_rank = rank by train_count descending
top_items = first 50,000 items
~~~

禁止：

~~~text
使用 valid/test 频次选择 item
使用全量 interactions 选择 item
使用 test item 调整 cap
~~~

### 数据泄漏规则

~~~text
collaborative embedding 只用 train interactions
user cluster 只用 train interactions
behavior profile 只用 train interactions
valid/test 只用于最终评测
~~~

## 43.3 今晚参数覆盖表

完整文档参数与今晚参数的唯一差异如下。

| 参数 | 完整 md | 今晚折中 | 原因 |
|---|---:|---:|---|
| interactions | 9.5M | 1M | 缩短 I/O 和评测 |
| users | 776K | 80K–120K | 控制数据规模 |
| items | 495K | 50K | 加速 SID 和 beam |
| RQ-VAE epochs | 20000 | 3000 | 控制训练时间 |
| RQ-VAE optimizer | Adagrad 0.4 | AdamW 5e-4 | 在缩短 epoch 下更稳定 |
| T5 steps | 200000 | 20K–50K | 单晚可行 |
| L2 tuning | validation 搜索 | 固定 3×256/latent32/beam20 | 不做完整调参 |
| seeds | 3–5 | 1（seed42） | 控制训练次数 |
| eval users | 776K | 2K–3K | 控制 beam 评测时间 |
| exhaustive users | 全量 | 500 | 只验证排序趋势 |
| min-cost-flow | 全量精确 | 分层或 greedy fallback | 控制求解时间 |

保持不变：

~~~text
3 semantic levels
codebook size 256
latent dim 32
SID length 4
user token 2000
beam 20
~~~

## 43.4 今晚固定模型配置

### RQ-VAE

~~~json
{
  "sid": {
    "num_semantic_levels": 3,
    "codebook_size": 256,
    "latent_dim": 32,
    "hidden_dim": 512,
    "dropout": 0.1,
    "epochs": 3000,
    "batch_size": 1024,
    "optimizer": "adamw",
    "learning_rate": 0.0005,
    "weight_decay": 0.00001,
    "commitment_weight": 0.25,
    "codebook_weight": 0.25,
    "kmeans_init": true,
    "kmeans_max_iter": 100,
    "length_mode": "fixed",
    "collision_mode": "suffix"
  }
}
~~~

### L2-lite baseline

~~~text
3 semantic levels
codebook 256
latent 32
SID length 4
user token 2000
beam 20
T5 d_model 128
T5 encoder/decoder 4/4
T5 heads 6
history 20
batch 256
bf16 true
train steps 20K–50K
~~~

### BC-GSID

~~~json
{
  "sid_allocation": {
    "enabled": true,
    "mode": "behavior_cluster_global",
    "solver": "hierarchical_min_cost_flow",
    "top_codes_per_level": 8,
    "base_candidates": 16,
    "suffix_candidates_per_original_code": 32,
    "lambda_recon": 1.0,
    "lambda_semantic": 0.2,
    "lambda_behavior": 0.1,
    "lambda_cluster": 0.1,
    "lambda_usage": 0.02,
    "lambda_pop": 0.0,
    "num_user_clusters": 32
  }
}
~~~

如果分层 min-cost-flow 超过 30 分钟：

~~~text
立即切换到 greedy_behavior_aware
并在报告中写明 overnight fallback
~~~

### UCG

~~~json
{
  "user_context": {
    "enabled": true,
    "fusion": "soft_prefix",
    "num_layers": 2,
    "num_heads": 4,
    "d_model": 128,
    "d_ff": 512,
    "dropout": 0.1,
    "short_window": 5,
    "long_window": 20,
    "num_soft_tokens": 4,
    "use_hashed_user_token": true
  }
}
~~~

### Joint

~~~json
{
  "joint_training": {
    "enabled": true,
    "freeze_codebooks": true,
    "joint_steps": 20000,
    "generator_lr": 0.0001,
    "rqvae_lr": 0.0001,
    "lambda_recon": 1.0,
    "lambda_next_sid": 1.0,
    "lambda_ranking": 0.1,
    "lambda_alignment": 0.1,
    "lambda_prefix": 0.05
  }
}
~~~

## 43.5 T5 训练步数选择

默认使用：

~~~text
train_steps = 20000
~~~

如果截至凌晨 1:00 的基准模型训练速度允许，则提升到：

~~~text
train_steps = 50000
~~~

所有模型必须使用相同值：

~~~text
L2-lite
BC-GSID
UCG
BC-GSID+UCG
~~~

禁止：

~~~text
baseline 用 20K
BC-GSID 用 50K
UCG 用 50K
~~~

如果时间不足，统一改为：

~~~text
30000 steps
~~~

## 43.6 今晚运行矩阵

| 编号 | 模型 | SID | 用户条件 | Joint | steps |
|---|---|---|---|---|---|
| O1 | L2-lite | suffix baseline | hashed user token | 否 | 20K–50K |
| O2 | BC-GSID | global behavior assignment | hashed user token | 否 | 20K–50K |
| O3 | UCG | suffix baseline | soft prefix | 否 | 20K–50K |
| O4 | BC-GSID+UCG | global behavior assignment | soft prefix | 否 | 20K–50K |
| O5 | Joint | BC-GSID | soft prefix | 20K fine-tune | 从 O4 继续 |

比较关系：

~~~text
O2 vs O1：验证 BC-GSID
O3 vs O1：验证 UCG
O4 vs O1：验证组合效果
O4 vs O2：验证用户条件是否额外有效
O4 vs O3：验证 SID allocation 是否额外有效
O5 vs O4：验证 Joint 是否额外有效
~~~


## 43.7 今晚时间表

假设：

~~~text
RTX 5090 32GB
bf16
batch 256
数据下载已完成
代码已具备最小可运行版本
~~~

| 时间 | 任务 | 输出 |
|---|---|---|
| 0:00–0:40 | Books 5-core 用户采样、top-50K item cap | final_data_stats.json |
| 0:40–1:10 | metadata 审计、train-only collaborative、user cluster | data audit |
| 1:10–1:50 | 50K item RQ-VAE 3000 epochs | rqvae.pt |
| 1:50–2:20 | baseline suffix SID | semantic_ids_baseline |
| 2:20–2:50 | BC-GSID 候选、分配、fallback | semantic_ids_bcgsid |
| 2:50–4:00 | O1 L2-lite generator 20K–50K steps | tiger_l2.pt |
| 4:00–5:10 | O2 BC-GSID generator | tiger_bcgsid.pt |
| 5:10–6:20 | O3 UCG generator | tiger_ucg.pt |
| 6:20–7:30 | O4 BC-GSID+UCG generator | tiger_bcgsid_ucg.pt |
| 7:30–7:50 | O5 Joint 20K fine-tune | tiger_joint.pt |
| 7:50–8:50 | 2K–3K users beam/trie 评测 | metrics |
| 8:50–9:20 | 500-user exhaustive 对照 | ranking audit |
| 9:20–10:00 | 汇总、置信区间、报告 | overnight report |

如果单次 generator 训练超过 70 分钟：

~~~text
立即把统一 train_steps 降为 20000
并把 eval users 降为 2000
~~~

如果 BC-GSID 求解超过 30 分钟：

~~~text
切换 greedy_behavior_aware
不再等待 exact solver
~~~

如果 RQ-VAE 超过 40 分钟：

~~~text
把 epochs 从 3000 降为 2000
所有 SID 必须用同一个 RQ-VAE checkpoint 重建
~~~

## 43.8 今晚评测协议

### 主评测

~~~text
test users = 2,000–3,000
beam = 20
method = standard beam + trie beam
metrics = Recall@10, Recall@20, NDCG@10, Coverage
latency = mean, P95, P99
~~~

### 辅助评测

~~~text
exhaustive ranking users = 500
purpose = 检查 beam 是否虚高
not for final paper claim
~~~

### 固定 test 用户集合

所有模型必须使用同一份 test users：

~~~text
test_users_seed = 42
sample_size = 2000–3000
stratify_by_history_length = true
~~~

禁止：

~~~text
baseline 用 3000 users
BC-GSID 用 2000 users
UCG 用随机其他 users
~~~

### 今晚必须输出的指标

~~~text
Recall@10
Recall@20
NDCG@10
Coverage
Invalid SID rate
Mean latency
P95 latency
P99 latency
Hits
Users
Semantic drift
Behavior drift
Cluster KL
SID length
Unique SIDs
Final collision rate
~~~

## 43.9 今晚必须生成的产物

~~~text
results/overnight/
├── sample_config.json
├── final_data_stats.json
├── rqvae/
│   ├── rqvae.pt
│   ├── training.json
│   └── utilization.json
├── sid/
│   ├── baseline/
│   │   ├── semantic_ids.json
│   │   └── sid_report.json
│   └── bcgsid/
│       ├── semantic_ids.json
│       ├── assignment_report.json
│       └── solver_report.json
├── generators/
│   ├── l2_lite.pt
│   ├── bcgsid.pt
│   ├── ucg.pt
│   ├── bcgsid_ucg.pt
│   └── joint.pt
├── eval/
│   ├── o1_l2_lite.json
│   ├── o2_bcgsid.json
│   ├── o3_ucg.json
│   ├── o4_bcgsid_ucg.json
│   ├── o5_joint.json
│   └── comparison.csv
└── overnight_report.md
~~~

## 43.10 今晚结果判定

### BC-GSID 有效信号

~~~text
Recall@20 相对 O1 提升 >= 2%
NDCG@10 相对 O1 提升 >= 2%
semantic drift <= O1 * 1.05
behavior drift 下降
shuffled behavior 对照不提升
~~~

### UCG 有效信号

~~~text
Recall@20 相对 O1 提升 >= 2%
shuffled user context 不提升
短历史用户至少提升 3%
~~~

### BC-GSID+UCG 有效信号

~~~text
超过 O2 和 O3
或至少达到二者中的最佳值
并且 semantic drift 不恶化
~~~

### Joint 有效信号

~~~text
相对 O4 Recall@20 提升 >= 1.5%
NDCG@10 提升 >= 1.5%
SID lookup 仍唯一
codebook utilization >= 0.8
~~~

如果没有达到阈值：

~~~text
不删除结果
标记为 overnight negative result
不得继续堆叠更多模块
先检查数据、SID drift 和训练预算
~~~

## 43.11 今晚可以写什么，不能写什么

可以写：

~~~text
single-seed overnight screening
BC-GSID 在此设置下是否出现有效信号
UCG 是否出现有效信号
组合是否优于单模块
Joint 是否有额外收益
~~~

不能写：

~~~text
论文最终结果
超越标准 TIGER 的结论
跨数据集稳健性
统计显著性的最终结论
长期泛化能力
~~~

## 43.12 明天必须补的实验

今晚结束后，按优先级补：

1. 对今晚最好的模块补 seed43、44；
2. 对 BC-GSID 和 UCG 分别补 random/shuffle 对照；
3. 把 eval users 从 2K–3K 扩到 20K–50K；
4. 把 item cap 从 50K 扩到 100K 或完整 495K；
5. 把 RQ-VAE 从 3000 epochs 恢复到 20000 epochs；
6. 把 T5 steps 从 20K–50K 恢复到 200K；
7. 补齐 L1 standard 和 L2 tuned；
8. 最后再做多数据集和论文级统计。

## 43.13 今晚执行原则

今晚只做一次主链：

~~~text
Books 5-core
-> 1M interactions
-> top-50K items
-> 3×256/latent32 RQ-VAE
-> suffix baseline + BC-GSID
-> seed42
-> L2-lite
-> BC-GSID
-> UCG
-> BC-GSID+UCG
-> Joint
-> 2K–3K beam/trie 评测
~~~

今晚不追加：

~~~text
额外数据集
额外 seed
完整 L2 网格
完整 495K item 评测
完整 776K user exhaustive 评测
额外搜索 beam、latent、levels
~~~

今晚的最终目标只有一个：

> 用同一份数据、同一个 seed、同一个预算，判断 BC-GSID、UCG、组合和 Joint 是否出现值得继续验证的正向信号。


---

# 44. 运行评测指标、输出文件与对比规范

> 本节是运行时必须采集和输出的指标规范。  
> 训练日志可以有很多诊断指标，但最终比较必须固定使用同一套检索指标、同一批 test users、同一个 beam 和同一个模型预算。

## 44.1 四类指标

运行时需要同时记录：

~~~text
1. RQ-VAE 训练指标
2. SID 构建与分配指标
3. Generator 训练指标
4. 检索、分组和工程效率指标
~~~

最终报告中：

- Recall/NDCG 决定方法是否有效；
- SID 指标决定提升是否来自合理的语义结构；
- 分组指标解释用户和商品收益来源；
- 延迟和显存决定方法是否具备工程可行性。

## 44.2 RQ-VAE 训练指标

输出文件：

~~~text
results/overnight/rqvae/training.json
results/overnight/rqvae/utilization.json
~~~

必须记录：

| 指标 | 含义 |
|---|---|
| loss | RQ-VAE 总损失 |
| reconstruction | 重建损失 |
| commitment | commitment loss |
| codebook | codebook loss |
| usage | soft usage 正则 |
| hard_usage | hard usage 正则 |
| epoch_utilization_level_i | 第 i 层码本利用率 |
| epoch_dead_codes_level_i | 第 i 层死码数量 |
| perplexity_level_i | 第 i 层码本 perplexity |
| dead_codes_reset | 本轮重置死码数量 |

重点判断：

~~~text
每层 utilization 是否合理
dead codes 是否持续下降
reconstruction 是否下降
是否出现某一层利用率接近 0
~~~

如果 codebook utilization 很低，即使 Recall 偶然提高，也不能认为 SID 学习可信。

## 44.3 SID 构建和 BC-GSID 指标

输出文件：

~~~text
results/overnight/sid/baseline/sid_report.json
results/overnight/sid/bcgsid/assignment_report.json
results/overnight/sid/bcgsid/solver_report.json
~~~

必须记录：

| 指标 | 含义 |
|---|---|
| items | SID 商品数量 |
| sid_length | SID 长度，今晚必须为 4 |
| unique_sids | 唯一 SID 数量 |
| raw_collision_rate | RQ-VAE 原始码冲突率 |
| final_collision_rate | 最终系统冲突率，必须为 0 |
| suffix_resolved | 使用 suffix 解决的 item 数 |
| semantic_reallocated | 使用语义重分配解决的 item 数 |
| mean_semantic_drift | SID 与内容 latent 的语义偏移 |
| mean_behavior_drift | SID 与行为画像的偏移 |
| mean_cluster_kl | SID 预测用户簇分布与真实分布的 KL |
| codebook_utilization | 每层最终利用率 |
| codebook_perplexity | 每层最终 perplexity |
| solver | min_cost_flow 或 greedy |
| solver_time_seconds | SID 分配求解耗时 |

重点比较：

~~~text
baseline suffix SID vs BC-GSID
~~~

必须确认：

- final collision 仍为 0；
- SID 长度仍为 4；
- vocab 不扩大；
- semantic drift 不恶化超过 5%；
- behavior drift 下降；
- cluster KL 下降或至少不恶化。

## 44.4 Generator 训练指标

输出文件：

~~~text
results/overnight/generators/l2_lite.pt
results/overnight/generators/bcgsid.pt
results/overnight/generators/ucg.pt
results/overnight/generators/bcgsid_ucg.pt
results/overnight/generators/joint.pt
~~~

必须记录：

| 指标 | 含义 |
|---|---|
| train_loss | 训练交叉熵 |
| valid_loss | 验证损失 |
| best_valid_loss | 最佳验证损失 |
| checkpoint_step | 最佳 checkpoint 步数 |
| elapsed_sec | 训练耗时 |
| user_context_loss | UCG 的 alignment loss |
| ranking_loss | Joint 的排序损失 |
| prefix_loss | Joint 的前缀一致性损失 |
| steps_per_sec | 训练速度 |
| GPU_memory_peak | GPU 峰值显存 |

注意：

~~~text
valid_loss 低不一定代表 Recall 高
最终 checkpoint 不能只用 valid_loss 选择
必须结合 validation Recall/NDCG
test 不能用于 checkpoint 选择
~~~

## 44.5 检索主指标

输出文件：

~~~text
results/overnight/eval/o1_l2_lite.json
results/overnight/eval/o2_bcgsid.json
results/overnight/eval/o3_ucg.json
results/overnight/eval/o4_bcgsid_ucg.json
results/overnight/eval/o5_joint.json
results/overnight/eval/comparison.csv
~~~

必须记录：

| 指标 | 含义 |
|---|---|
| Recall@10 | 前 10 个推荐中命中目标的比例 |
| Recall@20 | 前 20 个推荐中命中目标的比例 |
| NDCG@10 | 前 10 的归一化折损累计增益 |
| NDCG@20 | 前 20 的 NDCG |
| MRR@20 | 首个命中的倒数排名均值 |
| Coverage | 推荐过的 item 占 catalog 的比例 |
| Hits | 实际命中的用户数 |
| Users | 评测用户数 |
| invalid_sid_rate | 非法 SID 比例 |
| valid_sid_rate | 合法 SID 比例 |
| avg_returned_items | 平均返回的有效 item 数量 |
| seen_item_filtered | 过滤历史 item 的数量 |

计算定义：

~~~text
Recall@K = 命中用户数 / 评测用户数
NDCG@K = 每个命中用户的 1/log2(rank+1) 的平均
MRR@K = 每个命中用户 1/rank 的平均
Coverage = 被推荐过的唯一 item 数 / catalog item 数
Invalid SID rate = 非法 beam 数 / 总 beam 数
~~~

## 44.6 分组指标

必须按历史长度分组：

~~~text
history_length <= 5
6 <= history_length <= 20
history_length > 20
~~~

每组输出：

~~~text
Recall@10
Recall@20
NDCG@10
NDCG@20
Hits
Users
~~~

用户条件融合和 UCG 最应该改善短历史用户：

~~~text
history_length <= 5
~~~

如果收益只出现在长历史用户，说明可能只是用户记忆或流行度。

## 44.7 商品分组指标

建议输出：

~~~text
cold_item_recall@20
few_shot_item_recall@20
long_tail_recall@20
head_item_recall@20
~~~

定义：

~~~text
cold_item：训练期没有交互，测试期首次出现
few_shot_item：训练期 1–5 次交互
long_tail_item：训练频次处于后 20%
head_item：训练频次处于前 20%
~~~

注意：

今晚使用 top-50K item cap，因此 cold-item 指标只能作为诊断，不能作为最终论文结论。

## 44.8 工程效率指标

必须记录：

| 指标 | 含义 |
|---|---|
| mean_ms | 平均请求时延 |
| p50_ms | 中位时延 |
| p95_ms | P95 时延 |
| p99_ms | P99 时延 |
| wall_time_sec | 总评测耗时 |
| steps_per_sec | T5 训练速度 |
| GPU_memory_peak | 显存峰值 |
| solver_time_seconds | SID assignment 耗时 |

比较延迟时必须保证：

~~~text
相同 test users
相同 beam
相同 batch size
相同 max_history_items
相同硬件状态
~~~

## 44.9 标准 beam、trie beam 和 exhaustive ranking

需要分别输出：

~~~text
standard beam result
trie beam result
exhaustive subset result
~~~

标准 beam：

- 允许生成全部 SID token；
- 包含 conflict token；
- 生成后过滤非法 SID；
- 过滤历史 item；
- 按照生成分数排序。

trie beam：

- 只扩展合法前缀；
- invalid SID rate 预期为 0；
- 但不能把 0% invalid 当作主创新。

exhaustive ranking：

- 只用于 500 users；
- 用于检查 beam 是否虚高；
- 不用于今晚的主结论。

## 44.10 今晚标准比较表

最终必须生成：

| Metric | O1 L2-lite | O2 BC-GSID | O3 UCG | O4 BC-GSID+UCG | O5 Joint |
|---|---:|---:|---:|---:|---:|
| Recall@10 | — | — | — | — | — |
| Recall@20 | — | — | — | — | — |
| NDCG@10 | — | — | — | — | — |
| NDCG@20 | — | — | — | — | — |
| MRR@20 | — | — | — | — | — |
| Coverage | — | — | — | — | — |
| Hits | — | — | — | — | — |
| Users | 2K–3K | 2K–3K | 2K–3K | 2K–3K | 2K–3K |
| Invalid SID rate | — | — | — | — | — |
| Mean latency | — | — | — | — | — |
| P99 latency | — | — | — | — | — |
| Semantic drift | — | — | — | — | — |
| Behavior drift | — | — | — | — | — |
| Cluster KL | — | — | — | — | — |

比较规则：

~~~text
O2 vs O1：BC-GSID 是否有效
O3 vs O1：UCG 是否有效
O4 vs O1：组合是否有效
O4 vs O2：用户条件是否额外有效
O4 vs O3：SID allocation 是否额外有效
O5 vs O4：Joint 是否额外有效
~~~

## 44.11 结果判定标准

BC-GSID：

~~~text
Recall@20 相对 O1 提升 >= 2%
NDCG@10 相对 O1 提升 >= 2%
semantic drift <= O1 * 1.05
behavior drift 下降
shuffled behavior 不提升
~~~

UCG：

~~~text
Recall@20 相对 O1 提升 >= 2%
shuffled user context 不提升
短历史用户至少提升 3%
~~~

BC-GSID+UCG：

~~~text
超过 O2 和 O3
或至少达到二者中的最佳值
semantic drift 不恶化
~~~

Joint：

~~~text
相对 O4 Recall@20 提升 >= 1.5%
相对 O4 NDCG@10 提升 >= 1.5%
SID lookup 仍唯一
codebook utilization >= 0.8
~~~

如果没有达到阈值：

~~~text
标记为 overnight negative result
不能写成论文级结论
先检查数据、SID drift 和训练预算
不得靠增加 beam 或 SID 长度掩盖问题
~~~
